// ===== Interfaces =====
export interface ContentBlock {
  content: string;
  codeExamples?: string[];
}

export interface Topic {
  id?: string;
  name?: string;
  beginner?: ContentBlock[];
  intermediate?: ContentBlock[];
  expert?: ContentBlock[];
}

export interface SubTopic {
  id: string;
  name: string;
  topics?: Topic[];
}

export interface Course {
  id: string;
  name: string;
  icon: string;
  subTopics: SubTopic[];
  cheatNotes: string[];
  interviewQuestions: {
    company: string;
    questions: string[];
  }[];
}

// ===== Course Data =====
export const MongoDB: Course[] = [
  {
    id: "mongodb",
    name: "MongoDB",
    icon: "Database",

    subTopics: [
      {
        id: "mongodb-topics",
        name: "MongoDB Topics",
        topics: [
         {
  id: "mongodb-intro",
  name: "MongoDB Overview & Introduction",
   beginner: [
    {
      content: `
<b>MongoDB Introduction (Beginner Level)</b><br><br>

MongoDB is a <b>NoSQL database</b> used to store data in a simple and flexible way.<br>
It stores data in the form of <b>documents</b> instead of tables like traditional databases.<br><br>

MongoDB is developed by <b>MongoDB Inc.</b><br>
It is open-source and widely used in modern web applications.<br><br>

<hr>

<b>What is NoSQL?</b><br><br>

NoSQL means <b>Not Only SQL</b>.<br>
It does not use rows and tables like traditional relational databases.<br>
Instead, it stores data in flexible formats like documents.<br><br>

<hr>

<b>Basic MongoDB Structure</b><br><br>

<b>1. Database</b><br>
• A container that holds collections.<br>
• Example: StudentDB, EmployeeDB<br><br>

<b>2. Collection</b><br>
• A group of documents.<br>
• Similar to a table in SQL.<br><br>

<b>3. Document</b><br>
• A single record in MongoDB.<br>
• Stored in JSON-like format.<br><br>

<b>Example Document:</b><br>

{
  "name": "Pratiksha",
  "age": 22
}
<br><br>

<hr>

<b>Features of MongoDB</b><br><br>

• Easy to learn and use<br>
• Flexible structure (No fixed schema)<br>
• Stores data in JSON format<br>
• Good performance<br>
• Supports large amount of data<br><br>

<hr>

<b>Where MongoDB is Used?</b><br><br>

• Web Applications<br>
• Mobile Applications<br>
• Real-time Applications<br>
• E-commerce Websites<br><br>

<hr>

<b>Why Learn MongoDB?</b><br><br>

• Popular in modern development<br>
• Used with Node.js, React, and Spring Boot<br>
• High demand skill in software industry<br><br>
`
    }
  ],
  
  intermediate: [
    {
      content: `
<b>MongoDB Overview & Introduction</b><br><br>

MongoDB is a <b>NoSQL, document-oriented database</b> that stores data in flexible, JSON-like documents. 
It is designed to handle large volumes of data with <b>high performance, scalability, and availability</b>.<br><br>

MongoDB uses <b>BSON (Binary JSON)</b> format internally to store data, which supports additional data types and improves performance.<br><br>

<hr>

<b>MongoDB Architecture Components</b><br><br>

<b>1. Database</b><br>
• A database is a physical container for collections.<br>
• Each MongoDB instance can host Database.<br>
• Each database stores data in separate files.<br>
• Used to organize collections logically.<br><br>

<b>Example:</b><br>
StudentDB, EmployeeDB, BloodBankDB<br><br>

<hr>

<b>2. Collection</b><br>
• Collection is a group of MongoDB documents.<br>
• Equivalent to a <b>table</b> in relational databases.<br>
• Collections do not enforce strict schema rules.<br>
• Documents inside a collection can have different structures.<br><br>

<b>Example:</b><br>
Students Collection, Donors Collection, Orders Collection<br><br>

<hr>

<b>3. Document</b><br>
• Document is a set of key-value pairs.<br>
• Equivalent to a <b>row or record</b> in RDBMS.<br>
• Stored in BSON format.<br>
• Supports nested documents and arrays.<br>
• Provides dynamic schema flexibility.<br><br>

<b>Example Document:</b><br>

{
  "name": "Pratiksha",
  "age": 22,
  "skills": ["Java", "Spring Boot", "MongoDB"]
}
<br><br>

<hr>

<b>4. Fields</b><br>
• Fields are individual key-value pairs inside documents.<br>
• Similar to columns in relational databases.<br>
• Can store multiple data types like String, Number, Boolean, Array, and Object.<br><br>

<hr>

<b>5. _id Field (Primary Identifier)</b><br>
• Every document contains a unique <b>_id</b> field.<br>
• Automatically generated by MongoDB if not provided.<br>
• Used for uniquely identifying documents inside collection.<br>
• Works similar to Primary Key in RDBMS.<br><br>

<hr>

<b>MongoDB vs RDBMS Mapping</b><br><br>

Database → Database<br>
Table → Collection<br>
Row → Document<br>
Column → Field<br>
Primary Key → _id<br><br>

<hr>

<b>Key Advantages of MongoDB</b><br><br>

• Flexible and Schema-less Database<br>
• High Performance Read and Write Operations<br>
• Horizontal Scaling using Sharding<br>
• Supports Distributed Systems<br>
• Stores Complex and Nested Data Easily<br><br>

<hr>

<b>Common Use Cases</b><br><br>

• Real-time Analytics Applications<br>
• Content Management Systems<br>
• E-commerce Platforms<br>
• IoT Applications<br>
• Big Data Processing Systems
`
    }
  ],
  expert: [
  {
    content: `
<b>MongoDB Overview & Introduction (Expert Level)</b><br><br>

MongoDB is a distributed, document-oriented NoSQL database designed
for high performance, high availability, and horizontal scalability.<br><br>

It stores data in flexible, JSON-like BSON (Binary JSON) documents,
allowing dynamic schema design and rapid application development.<br><br>

<hr>

<b>1. Core Architecture</b><br><br>

MongoDB follows a distributed systems architecture built around:<br><br>

• Document Model (BSON storage format)<br>
• Replica Sets (High Availability)<br>
• Sharding (Horizontal Scaling)<br>
• WiredTiger Storage Engine (Default)<br><br>

The WiredTiger engine provides:<br>
• Document-level concurrency control<br>
• Compression (Snappy, Zlib)<br>
• Checkpointing & journaling<br><br>

<hr>

<b>2. Data Model (Advanced Understanding)</b><br><br>

MongoDB uses collections and documents instead of tables and rows.<br><br>

Key characteristics:<br>
• Schema-less (dynamic schema)<br>
• Nested documents and arrays<br>
• Polymorphic data support<br>
• Rich indexing capabilities<br><br>

BSON supports additional data types beyond JSON:<br>
• ObjectId<br>
• Date<br>
• Decimal128<br>
• Binary data<br>
• Embedded documents<br><br>

<hr>

<b>3. Query Engine & Execution</b><br><br>

MongoDB uses a query planner that selects optimal execution plans based on:<br>
• Available indexes<br>
• Collection statistics<br>
• Query shape<br><br>

Execution stages include:<br>
• COLLSCAN (Collection Scan)<br>
• IXSCAN (Index Scan)<br>
• FETCH<br>
• SORT<br><br>

MongoDB 5+ uses Slot-Based Execution (SBE) engine for improved performance.<br><br>

<hr>

<b>4. Aggregation Framework (Advanced)</b><br><br>

Aggregation Pipeline enables multi-stage data processing:<br>
• $match<br>
• $group<br>
• $project<br>
• $lookup (Join-like behavior)<br>
• $facet<br>
• $bucket<br>
• $merge<br><br>

It is optimized and preferred over MapReduce for most workloads.<br><br>

<hr>

<b>5. Indexing Strategies</b><br><br>

MongoDB supports multiple index types:<br>
• Single Field Index<br>
• Compound Index<br>
• Multikey Index<br>
• Text Index<br>
• Geospatial Index<br>
• Hashed Index<br>
• TTL Index<br><br>

Covered queries and index intersection improve performance.<br><br>

<hr>

<b>6. Replication & High Availability</b><br><br>

Replica Set Architecture:<br>
• Primary (writes)<br>
• Secondary (replication)<br>
• Arbiter (optional)<br><br>

Features:<br>
• Automatic failover<br>
• Oplog-based replication<br>
• Read preference configuration<br>
• Write concern levels<br><br>

<hr>

<b>7. Sharding & Horizontal Scaling</b><br><br>

Sharding distributes data across multiple nodes using a shard key.<br><br>

Components:<br>
• Shards<br>
• Config Servers<br>
• Mongos (Query Router)<br><br>

Supports:<br>
• Range-based sharding<br>
• Hashed sharding<br>
• Zone sharding<br><br>

<hr>

<b>8. Transactions & ACID Compliance</b><br><br>

MongoDB supports multi-document ACID transactions (since 4.0).<br><br>

Provides:<br>
• Snapshot isolation<br>
• Two-phase commit internally<br>
• Cross-shard transactions<br><br>

<hr>

<b>9. Security Architecture</b><br><br>

Enterprise-grade security includes:<br>
• Role-Based Access Control (RBAC)<br>
• SCRAM authentication<br>
• TLS/SSL encryption<br>
• Field-level encryption<br>
• Auditing<br><br>

<hr>

<b>10. Cloud & Managed Deployment</b><br><br>

MongoDB Atlas provides:<br>
• Fully managed clusters<br>
• Auto-scaling<br>
• Global distribution<br>
• Performance advisor<br>
• Continuous backups<br><br>

<hr>

<b>11. Ideal Use Cases</b><br><br>

• Microservices architectures<br>
• Real-time analytics<br>
• Content management systems<br>
• IoT platforms<br>
• E-commerce platforms<br>
• High-write workloads<br><br>

<hr>

<b>12. When NOT to Use MongoDB</b><br><br>

• Heavy complex JOIN-based relational systems<br>
• Strict normalized schema with heavy foreign key constraints<br><br>

<hr>

<b>Expert Summary</b><br><br>

MongoDB is a modern, distributed NoSQL database that balances
developer agility with enterprise-grade scalability and resilience.
It is optimized for flexible schema design, horizontal scaling,
and high-throughput workloads in cloud-native architectures.<br><br>
`
  }
]
},


         {
  id: "mongodb-data-modeling",
  name: "MongoDB Data Modeling",
  beginner: [
    {
      content: `
<b>MongoDB Data Modeling (Beginner Level)</b><br><br>

Data Modeling means designing how data will be stored inside MongoDB.<br>
It helps us organize data properly so that the application runs fast and efficiently.<br><br>

In MongoDB, data is stored in <b>documents</b> inside <b>collections</b>.<br>
Since MongoDB does not require a fixed schema, we can design documents based on application needs.<br><br>

<hr>

<b>Why Data Modeling is Important?</b><br><br>

• Improves performance<br>
• Makes data easy to read and manage<br>
• Reduces unnecessary queries<br>
• Helps in scaling the application<br><br>

<hr>

<b>Two Main Ways to Design Data in MongoDB</b><br><br>

<b>1. Embedding (Store Data Together)</b><br>
Embedding means storing related data inside the same document.<br>
It is useful when data is small and used together.<br><br>

<b>Example:</b><br>

{
  name: "Pratiksha",
  course: {
    courseName: "Java",
    duration: "6 Months"
  }
}
<br><br>

<b>When to Use Embedding?</b><br>
• When related data is small<br>
• When data is always used together<br>
• To improve read performance<br><br>

<hr>

<b>2. Referencing (Store Data Separately)</b><br>
Referencing means storing related data in different documents and linking them using an ID.<br>
It is similar to foreign key in relational databases.<br><br>

<b>Example:</b><br>

Student Document:<br>
{
  studentId: 1,
  name: "Pratiksha",
  courseId: 101
}
<br><br>

Course Document:<br>
{
  courseId: 101,
  courseName: "MongoDB"
}
<br><br>

<b>When to Use Referencing?</b><br>
• When data is large<br>
• When data is shared by many documents<br>
• To reduce duplication<br><br>

<hr>

<b>Simple Difference</b><br><br>

Embedding → Store related data in one document<br>
Referencing → Store related data in separate documents<br><br>

<hr>

<b>Best Practice (Beginner Tip)</b><br><br>

• Design schema based on how your application reads data<br>
• Keep documents simple and clean<br>
• Avoid very large documents<br><br>

<hr>

<b>Example Use Cases</b><br><br>

• Student and Course System<br>
• E-commerce Product and Reviews<br>
• Blog Posts and Comments<br><br>
`
    }
  ],
  intermediate: [
    {
      content: `
<b>MongoDB Data Modeling</b><br><br>

Data Modeling in MongoDB refers to the process of designing the structure of documents and collections to store and manage data efficiently.<br>
Unlike relational databases, MongoDB follows a <b>flexible schema design</b> that allows developers to structure data based on application requirements.<br><br>

MongoDB data modeling focuses on improving:<br>
• Performance<br>
• Scalability<br>
• Data Retrieval Speed<br>
• Application Flexibility<br><br>

<hr>

<b>Key Concepts of Data Modeling</b><br><br>

<b>1. Embedding (Embedded Documents)</b><br>
Embedding means storing related data inside a single document.<br>
It reduces the need for multiple queries and improves read performance.<br><br>

<b>Advantages:</b><br>
• Faster Data Retrieval<br>
• Reduces Joins<br>
• Improves Performance<br><br>

<b>Example:</b><br>

<pre>
{
  name: "Pratiksha",
  course: {
    courseName: "Java",
    duration: "6 Months"
  }
}
</pre>

<hr>

<b>2. Referencing (Document References)</b><br>
Referencing means storing data in separate documents and linking them using references.<br>
It is similar to foreign key relationships in relational databases.<br><br>

<b>Advantages:</b><br>
• Reduces Data Duplication<br>
• Maintains Data Consistency<br>
• Suitable for Large Data Sets<br><br>

<b>Example:</b><br>

<pre>
Student Document:
{
  studentId: 1,
  name: "Pratiksha",
  courseId: 101
}

Course Document:
{
  courseId: 101,
  courseName: "MongoDB"
}
</pre>

<hr>

<b>Embedding vs Referencing</b><br><br>

Embedding is used when:<br>
• Data is frequently accessed together<br>
• Data size is small<br>
• One-to-Few relationships exist<br><br>

Referencing is used when:<br>
• Data is large or complex<br>
• Data is shared across multiple documents<br>
• One-to-Many or Many-to-Many relationships exist<br><br>

<hr>

<b>Design Considerations in MongoDB Data Modeling</b><br><br>

<b>1. Application Query Patterns</b><br>
Design schema based on how data is accessed frequently.<br><br>

<b>2. Document Growth</b><br>
Avoid designing documents that grow excessively large.<br><br>

<b>3. Atomic Operations</b><br>
MongoDB supports atomic operations at document level.<br>
Embedding helps maintain atomic updates.<br><br>

<b>4. Data Duplication</b><br>
Sometimes duplication is allowed to improve performance.<br><br>

<hr>

<b>Best Practices for Data Modeling</b><br><br>

• Keep related data together whenever possible<br>
• Avoid excessive document nesting<br>
• Design schema based on application usage<br>
• Use indexing to improve query performance<br>
• Balance between embedding and referencing<br><br>

<hr>

<b>Real World Use Cases</b><br><br>

• E-commerce product catalog modeling<br>
• Social media posts with comments<br>
• Student-course management systems<br>
• Order and customer management systems
`
    }
  ],
  expert: [
  {
    content: `
<b>MongoDB Data Modeling (Expert Level)</b><br><br>

Data Modeling in MongoDB is the process of designing how data is structured,
stored, and related inside collections to optimize performance, scalability,
and maintainability.<br><br>

Unlike relational databases, MongoDB modeling is query-driven rather than schema-driven.<br><br>

<hr>

<b>1. Core Principle: Model Around Application Queries</b><br><br>

In MongoDB, schema design should be based on:<br>
• Most frequent queries<br>
• Read vs write patterns<br>
• Data growth rate<br>
• Access patterns<br><br>

Design rule:<br>
"Data that is accessed together should be stored together."<br><br>

<hr>

<b>2. Embedding vs Referencing (Advanced Decision Strategy)</b><br><br>

<b>Embedding (Denormalization)</b><br>
Store related data inside the same document.<br><br>

Advantages:<br>
• Single query retrieval<br>
• Atomic updates<br>
• Better read performance<br><br>

Limitations:<br>
• Document size limit (16MB)<br>
• Large array growth issues<br><br>

Best For:<br>
• One-to-One relationships<br>
• One-to-Few relationships<br><br>

<hr>

<b>Referencing (Normalization)</b><br>
Store related data in separate collections and reference using ObjectId.<br><br>

Advantages:<br>
• Avoid large documents<br>
• Independent scaling<br>
• Reusable shared data<br><br>

Best For:<br>
• One-to-Many (large datasets)<br>
• Many-to-Many relationships<br>
• Frequently updated related data<br><br>

<hr>

<b>3. Relationship Patterns</b><br><br>

• One-to-One → Embed<br>
• One-to-Few → Embed<br>
• One-to-Many → Reference<br>
• Many-to-Many → Reference with linking collection<br><br>

<hr>

<b>4. Schema Design Patterns</b><br><br>

• Subset Pattern → Store frequently accessed data embedded, rest separate.<br>
• Extended Reference Pattern → Duplicate minimal required fields.<br>
• Bucket Pattern → Group time-series data together.<br>
• Attribute Pattern → Flexible attributes storage.<br>
• Polymorphic Pattern → Store different document shapes in same collection.<br><br>

<hr>

<b>5. Handling Large Arrays</b><br><br>

Avoid unbounded arrays.<br>
Use referencing if array size can grow infinitely.<br><br>

Example Problem:<br>
User with unlimited comments → Should reference comments instead of embedding.<br><br>

<hr>

<b>6. Shard Key Consideration in Modeling</b><br><br>

When designing for sharded clusters:<br>
• Choose high-cardinality shard key<br>
• Avoid monotonically increasing values<br>
• Prevent hotspotting<br><br>

Shard key impacts:<br>
• Write distribution<br>
• Query routing<br>
• Cluster performance<br><br>

<hr>

<b>7. Index-Aware Modeling</b><br><br>

Model documents considering index usage:<br>
• Compound index ordering matters<br>
• Support covered queries<br>
• Avoid over-indexing<br><br>

Design should align with query patterns.<br><br>

<hr>

<b>8. Transactions vs Atomicity</b><br><br>

MongoDB guarantees atomicity at document level.<br>
Embedding helps maintain atomic operations.<br>
Multi-document transactions add overhead.<br><br>

Prefer embedding when atomic consistency is required.<br><br>

<hr>

<b>9. Performance Optimization Principles</b><br><br>

• Reduce number of joins ($lookup usage)<br>
• Minimize document growth<br>
• Avoid deep nesting<br>
• Optimize frequently filtered fields<br>
• Pre-compute frequently calculated values (denormalization)<br><br>

<hr>

<b>10. Common Modeling Mistakes</b><br><br>

• Blindly copying relational schema<br>
• Over-normalizing data<br>
• Using too many collections<br>
• Ignoring query patterns<br>
• Not planning for scaling<br><br>

<hr>

<b>Expert Summary</b><br><br>

MongoDB Data Modeling is a balance between performance,
flexibility, and scalability. Unlike relational modeling,
it prioritizes query efficiency and distributed system design.
Proper modeling directly impacts application performance,
index efficiency, and horizontal scalability.<br><br>
`
  }
]
},


         {
  id: "create-database",
  name: "Create Database in MongoDB",
   beginner: [
    {
      content: `
<b>Create Database in MongoDB (Beginner Level)</b><br><br>

In MongoDB, a database is used to store collections.<br>
You can create a database easily using a simple command.<br><br>

<hr>

<b>Step 1: Check Existing Databases</b><br><br>

Use the following command to see all databases:<br>

<pre>
show dbs
</pre>

<hr>

<b>Step 2: Create or Switch Database</b><br><br>

To create a new database, use the <b>use</b> command:<br>

<pre>
use StudentDB
</pre>

• If the database does not exist, MongoDB will create it.<br>
• If it already exists, MongoDB will switch to that database.<br><br>

<hr>

<b>Important Note</b><br><br>

MongoDB does not actually create the database until you insert data into it.<br>
You must create at least one collection or insert one document.<br><br>

<hr>

<b>Step 3: Create Collection (Optional)</b><br><br>

You can create a collection using:<br>

<pre>
db.createCollection("students")
</pre>

<hr>

<b>Step 4: Insert Data (Database Gets Created)</b><br><br>

<pre>
db.students.insertOne({
  name: "Pratiksha",
  age: 22
})
</pre>

Now the database is officially created.<br><br>

<hr>

<b>Verify Database</b><br><br>

Run again:<br>

<pre>
show dbs
</pre>

You will see StudentDB in the list.<br><br>

<hr>

<b>Summary</b><br><br>

• use DatabaseName → Create or switch database<br>
• Database is created only after inserting data<br>
• show dbs → Display all databases<br><br>
`
    }
  ],
  intermediate: [
    {
      content: `
<b>Create Database in MongoDB</b><br><br>

MongoDB automatically creates a database when you start using it. 
Unlike relational databases, there is no separate CREATE DATABASE command required.<br><br>

A database is created when:<br>
• You switch to a new database using the <b>use</b> command<br>
• You insert data into that database<br><br>

<hr>

<b>1. Check Existing Databases</b><br><br>

Use the following command to display all databases:<br>

<pre>
show dbs
</pre>

<hr>

<b>2. Create or Switch Database</b><br><br>

Use the <b>use</b> command to create or switch to a database:<br>

<pre>
use StudentDB
</pre>

If the database does not exist, MongoDB prepares it for creation.<br>
The database will be created only after inserting data.<br><br>

<hr>

<b>3. Insert Data to Create Database</b><br><br>

To permanently create the database, insert a document into a collection:<br>

<pre>
db.students.insertOne({
  name: "Pratiksha",
  age: 22,
  city: "Pune"
})
</pre>

Now the database and collection will be created automatically.<br><br>

<hr>

<b>4. Verify Database Creation</b><br><br>

Run the command again to confirm database creation:<br>

<pre>
show dbs
</pre>

<hr>

<b>Important Points</b><br><br>

• MongoDB follows dynamic database creation.<br>
• Database is created only when it contains data.<br>
• Each database contains multiple collections.<br>
• Collections store documents in BSON format.<br><br>

<hr>

<b>Real Example Flow</b><br><br>

Step 1: Check Databases<br>
<pre>
show dbs
</pre>

Step 2: Create Database<br>
<pre>
use CollegeDB
</pre>

Step 3: Insert Data<br>
<pre>
db.students.insertOne({name:"Rahul"})
</pre>

Step 4: Verify<br>
<pre>
show dbs
</pre>
`
    }
  ],
  expert: [
  {
    content: `
<b>Create Database in MongoDB (Expert Level)</b><br><br>

In MongoDB, databases are created lazily (on demand).
There is no explicit CREATE DATABASE command as in relational systems.
A database is physically created only when the first write operation occurs.<br><br>

<hr>

<b>1. Logical vs Physical Creation</b><br><br>

• Logical creation → use databaseName<br>
• Physical creation → First insert/write operation<br><br>

Example:<br>
<pre>
use CollegeDB
</pre>

This only switches context. The database does NOT yet exist on disk.<br><br>

Database is actually created when:<br>

<pre>
db.students.insertOne({ name: "Pratiksha" })
</pre>

At this point:<br>
• Namespace is created<br>
• Collection metadata is initialized<br>
• Storage files are allocated<br><br>

<hr>

<b>2. Storage Engine Behavior (WiredTiger)</b><br><br>

MongoDB uses WiredTiger storage engine (default).<br><br>

When first document is inserted:<br>
• Collection file is created<br>
• Journal entry is recorded<br>
• Checkpoint mechanism ensures durability<br><br>

Database existence depends on:<br>
• Presence of collection data files<br>
• Metadata registration<br><br>

<hr>

<b>3. Database Metadata</b><br><br>

Each database maintains:<br>
• System collections<br>
• Index metadata<br>
• Collection namespace entries<br><br>

You can verify databases using:<br>

<pre>
show dbs
</pre>

Important:<br>
Empty databases are not displayed.<br><br>

<hr>

<b>4. Creating Database with Specific Options</b><br><br>

While database itself has no creation options,
collections inside it can define storage configuration.<br><br>

Example (Create Collection with Options):<br>

<pre>
db.createCollection("students", {
   capped: false
})
</pre>

This implicitly ensures database creation if it doesn't exist.<br><br>

<hr>

<b>5. Creating Database in Replica Set</b><br><br>

In a replica set:<br>
• Database creation occurs on Primary node<br>
• Oplog replicates creation to Secondary nodes<br>
• Ensures eventual consistency<br><br>

Write concern can be specified:<br>

<pre>
db.students.insertOne(
   { name: "Pratiksha" },
   { writeConcern: { w: "majority" } }
)
</pre>

This ensures database creation is replicated to majority nodes.<br><br>

<hr>

<b>6. Database Creation in Sharded Cluster</b><br><br>

In sharded architecture:<br>
• Mongos routes initial write<br>
• Metadata stored in config servers<br>
• Database primary shard is selected automatically<br><br>

You can manually assign primary shard:<br>

<pre>
sh.enableSharding("CollegeDB")
</pre>

<hr>

<b>7. Security & Authorization Considerations</b><br><br>

To create a database, user must have privileges:<br>
• readWrite role<br>
• dbAdmin role<br>
• clusterAdmin (advanced cases)<br><br>

MongoDB enforces Role-Based Access Control (RBAC).<br><br>

<hr>

<b>8. Production Best Practices</b><br><br>

• Avoid creating unnecessary databases.<br>
• Follow naming conventions (lowercase, no spaces).<br>
• Separate databases per microservice if required.<br>
• Monitor storage growth.<br>
• Plan shard key before scaling.<br><br>

<hr>

<b>9. Internals Summary</b><br><br>

Database creation in MongoDB is:
• Lazy-initialized
• Write-triggered
• Storage-engine dependent
• Fully replicated in distributed setup
• Metadata-driven in sharded clusters<br><br>

<hr>

<b>Expert Conclusion</b><br><br>

MongoDB does not explicitly create databases via DDL statements.
Instead, it follows a dynamic, write-driven creation model aligned
with its schema-flexible and distributed architecture design.
Understanding internal behavior (replication, journaling,
metadata management) is critical in production environments.<br><br>
`
  }
]
},
{
  id: "drop-database",
  name: "Drop Database in MongoDB",
   beginner: [
    {
      content: `
<b>Drop Database in MongoDB (Beginner Level)</b><br><br>

Dropping a database means deleting the entire database permanently.<br>
All collections and documents inside that database will be removed.<br><br>

⚠ <b>Warning:</b> This action cannot be undone.<br><br>

<hr>

<b>Step 1: Check Current Database</b><br><br>

To see the current database, use:<br>

<pre>
db
</pre>

<hr>

<b>Step 2: Switch to the Database You Want to Delete</b><br><br>

Use the <b>use</b> command:<br>

<pre>
use StudentDB
</pre>

<hr>

<b>Step 3: Drop the Database</b><br><br>

Use the following command:<br>

<pre>
db.dropDatabase()
</pre>

If successful, MongoDB will return:<br>

<pre>
{ "dropped" : "StudentDB", "ok" : 1 }
</pre>

<hr>

<b>Step 4: Verify</b><br><br>

Check all databases again:<br>

<pre>
show dbs
</pre>

The deleted database will no longer appear in the list.<br><br>

<hr>

<b>Summary</b><br><br>

• use DatabaseName → Switch to database<br>
• db.dropDatabase() → Delete database<br>
• show dbs → Verify deletion<br><br>
`
    }
  ],
  intermediate: [
    {
      content: `
<b>Drop Database in MongoDB</b><br><br>

Dropping a database means permanently deleting the selected database along with all collections and documents stored inside it.<br><br>

MongoDB provides a simple command to remove a database.<br><br>

<hr>

<b>1. Switch to Database</b><br><br>

Before dropping a database, you must switch to the database that you want to delete.<br>

<pre>
use StudentDB
</pre>

<hr>

<b>2. Drop Database Command</b><br><br>

Use the following command to delete the current database:<br>

<pre>
db.dropDatabase()
</pre>

This command deletes:<br>
• Database<br>
• All Collections<br>
• All Documents inside collections<br><br>

<hr>

<b>3. Verify Database Deletion</b><br><br>

To check whether database is deleted, use:<br>

<pre>
show dbs
</pre>

If the database is not displayed, it means it has been removed successfully.<br><br>

<hr>

<b>Important Points</b><br><br>

• Database deletion is permanent and cannot be recovered.<br>
• Always verify database name before dropping.<br>
• The command deletes entire database structure.<br>
• You must be connected to the database you want to delete.<br><br>

<hr>

<b>Real Example</b><br><br>

Step 1: Check Databases<br>
<pre>
show dbs
</pre>

Step 2: Switch Database<br>
<pre>
use CollegeDB
</pre>

Step 3: Drop Database<br>
<pre>
db.dropDatabase()
</pre>

Step 4: Verify<br>
<pre>
show dbs
</pre>
`
    }
  ],
  
  expert: [
    {
      content: `
<b>Drop Database in MongoDB (Expert Level)</b><br><br>

Dropping a database in MongoDB permanently removes:
• All collections
• All documents
• All indexes
• All metadata associated with that database<br><br>

It is an irreversible operation and should be handled carefully in production environments.<br><br>

<hr>

<b>1. Internal Working of db.dropDatabase()</b><br><br>

<pre>
db.dropDatabase()
</pre>

When executed:
• MongoDB removes all collection files from storage engine (WiredTiger by default)
• Deletes related index files
• Updates system catalog metadata
• Replicates the drop operation to secondary nodes (in replica sets)<br><br>

Return Example:
<pre>
{ "dropped" : "CollegeDB", "ok" : 1 }
</pre>

<hr>

<b>2. Behavior in Replica Sets</b><br><br>

• Operation must be executed on PRIMARY node.<br>
• The drop operation is written to the oplog.<br>
• Secondary nodes automatically replicate and apply the drop.<br>
• Rollback may occur if primary steps down during operation.<br><br>

Check primary:
<pre>
rs.status()
</pre>

<hr>

<b>3. Behavior in Sharded Clusters</b><br><br>

In sharded environments:
• Database metadata is stored in config servers.<br>
• Dropping a database removes shard metadata.<br>
• All collections across shards are deleted.<br>
• Operation is coordinated by mongos router.<br><br>

Command through mongos:
<pre>
use CollegeDB
db.dropDatabase()
</pre>

<hr>

<b>4. Storage Engine Considerations (WiredTiger)</b><br><br>

• Disk space may not be immediately returned to OS.<br>
• Space becomes reusable internally.<br>
• For full disk cleanup, compact or repair may be required.<br><br>

<hr>

<b>5. Alternative Method (Using Command)</b><br><br>

<pre>
db.runCommand({ dropDatabase: 1 })
</pre>

Used in scripts or automation tools.<br><br>

<hr>

<b>6. Security & Permissions</b><br><br>

Required Role:
• dbAdmin or root role<br>

Check current user roles:
<pre>
db.runCommand({ connectionStatus: 1 })
</pre>

Without proper privilege, operation will fail.<br><br>

<hr>

<b>7. Production Safety Checklist</b><br><br>

✔ Take backup before dropping<br>
✔ Verify correct environment (dev/staging/prod)<br>
✔ Confirm database name carefully<br>
✔ Ensure no active application dependency<br>
✔ Use role-based access control<br><br>

Backup Example:
<pre>
mongodump --db CollegeDB --out /backup/
</pre>

<hr>

<b>8. System Databases (Do NOT Drop)</b><br><br>

• admin → Authentication & cluster control<br>
• local → Replication data<br>
• config → Sharding metadata<br><br>

Dropping these can break the entire MongoDB instance.<br><br>

<hr>

<b>Expert Insight</b><br><br>

In large-scale production systems, dropping a database is typically:
• Restricted by strict RBAC policies<br>
• Logged and audited<br>
• Performed during maintenance windows<br>
• Often replaced by logical cleanup strategies instead of full drop<br><br>

<hr>

<b>Conclusion</b><br><br>

db.dropDatabase() is a lightweight command syntactically,
but operationally it affects storage engine, replication, sharding metadata,
security roles, and cluster consistency.<br><br>

Use with extreme caution in production environments.
`
    }
  ]

},
{
  id: "create-collection",
  name: "Create Collection in MongoDB",
  beginner: [
    {
      content: `
<b>Create Collection in MongoDB (Beginner Level)</b><br><br>

A Collection in MongoDB is a group of documents.<br>
It is similar to a table in relational databases.<br><br>

There are two ways to create a collection in MongoDB:<br><br>

<hr>

<b>Method 1: Automatically Create Collection</b><br><br>

MongoDB automatically creates a collection when you insert data into it.<br><br>

<pre>
use StudentDB

db.students.insertOne({
  name: "Pratiksha",
  age: 22
})
</pre>

If the "students" collection does not exist, MongoDB will create it automatically.<br><br>

<hr>

<b>Method 2: Manually Create Collection</b><br><br>

You can create a collection using the <b>createCollection()</b> method.<br><br>

<pre>
use StudentDB

db.createCollection("students")
</pre>

If successful, MongoDB returns:<br>

<pre>
{ "ok" : 1 }
</pre>

<hr>

<b>View All Collections</b><br><br>

To see all collections in the current database:<br>

<pre>
show collections
</pre>

<hr>

<b>Summary</b><br><br>

• use DatabaseName → Switch database<br>
• db.createCollection("name") → Create collection manually<br>
• insertOne() → Creates collection automatically if not exists<br>
• show collections → Display all collections<br><br>
`
    }
  ],
  intermediate: [
    {
      content: `
<b>Create Collection in MongoDB</b><br><br>

A collection in MongoDB is a group of documents. 
It is similar to a table in relational databases.<br><br>

MongoDB allows collections to be created in two ways:<br>
• Automatically<br>
• Manually using command<br><br>

<hr>

<b>1. Automatic Collection Creation</b><br><br>

MongoDB automatically creates a collection when you insert a document into a non-existing collection.<br><br>

<b>Example:</b><br>

<pre>
use StudentDB

db.students.insertOne({
  name: "Pratiksha",
  age: 22
})
</pre>

If the <b>students</b> collection does not exist, MongoDB creates it automatically.<br><br>

<hr>

<b>2. Manual Collection Creation</b><br><br>

You can explicitly create a collection using the following command:<br>

<pre>
db.createCollection("students")
</pre>

This command creates an empty collection.<br><br>

<hr>

<b>3. Create Collection with Options</b><br><br>

MongoDB allows creation of collections with configuration options.<br><br>

<b>Example:</b><br>

<pre>
db.createCollection("students", {
  capped: true,
  size: 5242880,
  max: 5000
})
</pre>

<b>Options Explanation:</b><br>
• capped → Fixed-size collection<br>
• size → Maximum collection size in bytes<br>
• max → Maximum number of documents allowed<br><br>

<hr>

<b>4. Show Collections</b><br><br>

To display all collections in current database:<br>

<pre>
show collections
</pre>

<hr>

<b>Important Points</b><br><br>

• Collections store MongoDB documents.<br>
• MongoDB collections are schema-less.<br>
• Documents inside collection can have different structures.<br>
• Automatic collection creation is commonly used.<br><br>

<hr>

<b>Real Example Flow</b><br><br>

Step 1: Switch Database<br>
<pre>
use CollegeDB
</pre>

Step 2: Create Collection<br>
<pre>
db.createCollection("students")
</pre>

Step 3: Verify Collection<br>
<pre>
show collections
</pre>
`
    }
  ],
  expert: [
  {
    content: `
<b>Create Collection in MongoDB (Expert Level)</b><br><br>

In MongoDB, collections are schema-flexible containers for documents.
In production systems, collections are created with validation rules,
collation settings, storage configuration, indexing strategy, and
specialized options like capped or time-series collections.<br><br>

<hr>

<b>1️⃣ Full Syntax</b><br><br>

<pre>
db.createCollection(name, options)
</pre>

• name → Collection name  
• options → Advanced configuration object<br><br>

<hr>

<b>2️⃣ Create Collection with Schema Validation (Production Standard)</b><br><br>

<pre>
use BankingDB

db.createCollection("customers", {
  validator: {
    $jsonSchema: {
      bsonType: "object",
      required: ["customerId", "name", "email", "balance"],
      properties: {
        customerId: {
          bsonType: "int",
          description: "must be an integer and is required"
        },
        name: {
          bsonType: "string",
          minLength: 3
        },
        email: {
          bsonType: "string",
          pattern: "^.+@.+$"
        },
        balance: {
          bsonType: "double",
          minimum: 0
        },
        accountType: {
          enum: ["Savings", "Current", "Loan"]
        },
        createdAt: {
          bsonType: "date"
        }
      }
    }
  },
  validationLevel: "strict",
  validationAction: "error"
})
</pre>

<b>Explanation:</b><br>
• $jsonSchema → Enforces structure and datatype validation  
• validationLevel: "strict" → Validate all inserts & updates  
• validationAction: "error" → Reject invalid documents<br><br>

<hr>

<b>3️⃣ Create Capped Collection (High-Performance Logging)</b><br><br>

<pre>
db.createCollection("transactionLogs", {
  capped: true,
  size: 10485760,
  max: 10000
})
</pre>

• Fixed size collection  
• Automatically overwrites oldest documents  
• Used in auditing & log systems<br><br>

<hr>

<b>4️⃣ Create Time-Series Collection (MongoDB 5.0+)</b><br><br>

<pre>
db.createCollection("atmTransactions", {
  timeseries: {
    timeField: "timestamp",
    metaField: "atmId",
    granularity: "minutes"
  }
})
</pre>

Used for:<br>
• IoT data  
• Financial transaction tracking  
• Real-time analytics<br><br>

<hr>

<b>5️⃣ Create Collection with Collation (Case-Insensitive Queries)</b><br><br>

<pre>
db.createCollection("users", {
  collation: {
    locale: "en",
    strength: 2
  }
})
</pre>

• strength: 2 → Case-insensitive matching  
• Useful for search systems<br><br>

<hr>

<b>6️⃣ Storage Engine & Index Considerations</b><br><br>

After creating collection, create indexes for performance:<br>

<pre>
db.customers.createIndex({ email: 1 }, { unique: true })
db.customers.createIndex({ balance: -1 })
</pre>

Expert Best Practices:<br>
✔ Always define schema validation in production  
✔ Use unique indexes for business keys  
✔ Avoid large unbounded documents  
✔ Monitor with db.collection.stats()  
✔ Use TTL indexes for auto-expiring data  

<hr>

<b>7️⃣ Check Collection Metadata</b><br><br>

<pre>
db.getCollectionInfos()
db.customers.stats()
</pre>

<hr>

<b>Enterprise Insight:</b><br><br>

In enterprise systems (Banking, E-commerce, Healthcare), manual
collection creation with strict validation and indexing strategy is mandatory.
Auto-creation via insertOne() is generally avoided in production.<br><br>
`
  }
],
},

{
  id: "drop-collection",
  name: "Drop Collection in MongoDB",
  beginner: [
    {
      content: `
<b>Drop Collection in MongoDB (Beginner Level)</b><br><br>

Dropping a collection means deleting the entire collection permanently.<br>
All documents inside that collection will be removed.<br><br>

⚠ <b>Warning:</b> This operation cannot be undone.<br><br>

<hr>

<b>Step 1: Switch to Database</b><br><br>

First, switch to the database where the collection exists:<br>

<pre>
use StudentDB
</pre>

<hr>

<b>Step 2: Drop the Collection</b><br><br>

Use the following command:<br>

<pre>
db.students.drop()
</pre>

If successful, MongoDB returns:<br>

<pre>
true
</pre>

<hr>

<b>Step 3: Verify Collection</b><br><br>

To check available collections:<br>

<pre>
show collections
</pre>

The deleted collection will not appear in the list.<br><br>

<hr>

<b>Alternative Method</b><br><br>

You can also drop collection using:<br>

<pre>
db.collectionName.drop()
</pre>

Example:<br>

<pre>
db.employees.drop()
</pre>

<hr>

<b>Summary</b><br><br>

• use DatabaseName → Switch database<br>
• db.collectionName.drop() → Delete collection<br>
• show collections → Verify deletion<br><br>
`
    }
  ],
  intermediate: [
    {
      content: `
<b>Drop Collection in MongoDB</b><br><br>

Dropping a collection means permanently deleting a collection along with all its documents from the database.<br><br>

This operation removes:<br>
• The collection structure<br>
• All documents inside the collection<br>
• All indexes associated with the collection<br><br>

<hr>

<b>1. Syntax to Drop Collection</b><br><br>

Use the following command to delete a collection:<br>

<pre>
db.collectionName.drop()
</pre>

<hr>

<b>2. Example</b><br><br>

If you want to delete the <b>students</b> collection:<br>

<pre>
use StudentDB

db.students.drop()
</pre>

If the collection exists, MongoDB returns:<br>
true<br><br>

If the collection does not exist, it returns:<br>
false<br><br>

<hr>

<b>3. Verify Collection Deletion</b><br><br>

To check available collections in current database:<br>

<pre>
show collections
</pre>

If the collection name is not listed, it has been successfully removed.<br><br>

<hr>

<b>Important Points</b><br><br>

• Collection deletion is permanent and cannot be undone.<br>
• It removes all data inside the collection.<br>
• Indexes related to the collection are also deleted.<br>
• Always verify collection name before dropping.<br><br>

<hr>

<b>Real Example Flow</b><br><br>

Step 1: Show Collections<br>
<pre>
show collections
</pre>

Step 2: Drop Collection<br>
<pre>
db.students.drop()
</pre>

Step 3: Verify<br>
<pre>
show collections
</pre>
`
    }
  ],
  expert: [
  {
    content: `
<b>Drop Collection in MongoDB (Expert Level – Deep Technical)</b><br><br>

Dropping a collection in MongoDB completely removes:
• All documents
• All indexes
• Collection metadata
• Storage allocation from the storage engine

This operation is irreversible and should be handled carefully in production systems.<br><br>

<hr>

<b>1️⃣ Core Syntax</b><br><br>

<pre>
db.collectionName.drop()
</pre>

Example:

<pre>
use BankingDB

db.auditLogs.drop()
</pre>

Return Value:
• true  → Collection successfully dropped
• false → Collection did not exist<br><br>

<hr>

<b>2️⃣ Internal Behavior (Storage Engine Level)</b><br><br>

When a collection is dropped:
• WiredTiger removes data files associated with the collection
• All index files are deleted
• Collection metadata is removed from system namespaces
• Locks are acquired to ensure atomicity

The operation is atomic at the collection level.<br><br>

<hr>

<b>3️⃣ Drop via Command API (Administrative Use)</b><br><br>

<pre>
db.runCommand({
  drop: "auditLogs"
})
</pre>

Used in:
• Migration scripts
• DevOps automation
• Deployment pipelines<br><br>

<hr>

<b>4️⃣ Behavior in Distributed Environments</b><br><br>

Replica Set:
• Drop operation replicates to secondaries
• Logged in oplog
• Guaranteed eventual consistency

Sharded Cluster:
• Metadata removed from config servers
• Collection dropped from all shards
• Balancer stops operations for that namespace

Requires appropriate privileges:
• dropCollection role
• dbOwner or higher<br><br>

<hr>

<b>5️⃣ Performance Considerations</b><br><br>

• Faster than deleteMany({}) for large collections
• Immediately frees namespace
• May cause brief lock contention
• Recommended for full data reset instead of bulk deletion<br><br>

<hr>

<b>6️⃣ Difference: drop() vs deleteMany({}) vs dropDatabase()</b><br><br>

drop():
• Removes collection + indexes

deleteMany({}):
• Removes documents only
• Keeps indexes and structure

dropDatabase():
• Removes entire database including all collections<br><br>

<hr>

<b>7️⃣ Production Safety Best Practices</b><br><br>

✔ Always verify current database using:
<pre>db</pre>

✔ Take backup before dropping production collections  
✔ Use environment-specific connection strings  
✔ Restrict drop permissions via RBAC  
✔ Log and audit destructive operations  
✔ Avoid running in primary peak traffic window<br><br>

<hr>

<b>8️⃣ Enterprise Use Cases</b><br><br>

• Clearing log collections before archiving
• Recreating collection with new validation schema
• CI/CD deployment reset
• Removing deprecated microservice data
• Data retention policy enforcement<br><br>

<hr>

<b>Expert Insight:</b><br><br>

In high-scale systems, instead of dropping active collections,
teams often:
• Rename old collection
• Create new validated collection
• Migrate data gradually
• Drop old collection after verification

This ensures zero-downtime deployment strategy.
`
  }
]
},

{
  id: "mongodb-datatypes",
  name: "MongoDB Data Types",
   beginner: [
    {
      content: `
<b>MongoDB Data Types (Beginner Level)</b><br><br>

MongoDB stores data in BSON (Binary JSON) format.<br>
Each field in a document has a specific data type.<br><br>

<hr>

<b>Common MongoDB Data Types</b><br><br>

<b>1. String</b><br>
• Used to store text values.<br>

Example:<br>
<pre>
{ name: "Pratiksha" }
</pre>

<hr>

<b>2. Integer (Number)</b><br>
• Used to store numeric values.<br>

Example:<br>
<pre>
{ age: 22 }
</pre>

<hr>

<b>3. Double</b><br>
• Used to store decimal numbers.<br>

Example:<br>
<pre>
{ salary: 25000.75 }
</pre>

<hr>

<b>4. Boolean</b><br>
• Stores true or false values.<br>

Example:<br>
<pre>
{ isActive: true }
</pre>

<hr>

<b>5. Array</b><br>
• Stores multiple values in a single field.<br>

Example:<br>
<pre>
{ skills: ["Java", "MongoDB", "Spring Boot"] }
</pre>

<hr>

<b>6. Object (Embedded Document)</b><br>
• Stores nested documents inside a document.<br>

Example:<br>
<pre>
{
  address: {
    city: "Pune",
    pincode: 411001
  }
}
</pre>

<hr>

<b>7. Null</b><br>
• Represents empty or no value.<br>

Example:<br>
<pre>
{ middleName: null }
</pre>

<hr>

<b>8. Date</b><br>
• Used to store date and time.<br>

Example:<br>
<pre>
{ createdAt: new Date() }
</pre>

<hr>

<b>9. ObjectId</b><br>
• Unique identifier automatically generated for each document.<br>
• Stored in _id field.<br><br>

Example:<br>
<pre>
{ _id: ObjectId("507f1f77bcf86cd799439011") }
</pre>

<hr>

<b>Summary</b><br><br>

• MongoDB uses BSON format.<br>
• Supports String, Number, Boolean, Array, Object, Date, Null, ObjectId.<br>
• Data types help store different kinds of values correctly.<br><br>
`
    }
  ],
  intermediate: [
    {
      content: `
<b>MongoDB Data Types</b><br><br>

MongoDB stores data in <b>BSON (Binary JSON)</b> format.
BSON supports multiple data types that extend JSON capabilities.<br><br>

<hr>

<b>1. String</b><br>
• Used to store text values.<br>
• Must be enclosed in double quotes.<br><br>

<b>Example:</b><br>
<pre>
{ name: "Pratiksha" }
</pre>

<hr>

<b>2. Integer</b><br>
• Used to store numeric values without decimals.<br>
• Can be 32-bit or 64-bit depending on value size.<br><br>

<b>Example:</b><br>
<pre>
{ age: 22 }
</pre>

<hr>

<b>3. Double</b><br>
• Used to store floating-point numbers (decimal values).<br><br>

<b>Example:</b><br>
<pre>
{ percentage: 89.75 }
</pre>

<hr>

<b>4. Boolean</b><br>
• Stores true or false values.<br><br>

<b>Example:</b><br>
<pre>
{ isActive: true }
</pre>

<hr>

<b>5. Array</b><br>
• Used to store multiple values in a single field.<br>
• Values are enclosed in square brackets [].<br><br>

<b>Example:</b><br>
<pre>
{ skills: ["Java", "Spring Boot", "MongoDB"] }
</pre>

<hr>

<b>6. Object (Embedded Document)</b><br>
• Used to store nested documents inside a document.<br><br>

<b>Example:</b><br>
<pre>
{
  address: {
    city: "Pune",
    pincode: 411001
  }
}
</pre>

<hr>

<b>7. Null</b><br>
• Represents empty or no value.<br><br>

<b>Example:</b><br>
<pre>
{ middleName: null }
</pre>

<hr>

<b>8. ObjectId</b><br>
• Default unique identifier for each document.<br>
• Automatically generated for _id field.<br><br>

<b>Example:</b><br>
<pre>
{ _id: ObjectId("65a123abc456def789012345") }
</pre>

<hr>

<b>9. Date</b><br>
• Used to store date and time values.<br><br>

<b>Example:</b><br>
<pre>
{ createdAt: new Date() }
</pre>

<hr>

<b>10. Timestamp</b><br>
• Used for internal MongoDB operations.<br>
• Stores time-related information.<br><br>

<hr>

<b>Important Points</b><br><br>

• MongoDB is schema-less but supports rich data types.<br>
• Data types improve query performance and validation.<br>
• BSON supports more types than standard JSON.<br>
• Choosing correct data type improves database efficiency.<br>
`
    }
  ],
  expert: [
  {
    content: `
<b>MongoDB Data Types (Expert Level Guide)</b><br><br>

MongoDB stores data in BSON (Binary JSON) format.
Each field in a document has a specific BSON type.
Understanding BSON types is critical for schema design,
index optimization, aggregation pipelines, and performance tuning.<br><br>

<hr>

<b>1️⃣ Core BSON Data Types</b><br><br>

<b>1. String</b><br>
• UTF-8 encoded string<br>
• Used for text values<br>
Example:
<pre>
{ name: "Pratiksha" }
</pre>

<b>2. Integer</b><br>
• int32 (NumberInt)<br>
• int64 (NumberLong)<br>
Example:
<pre>
{ age: NumberInt(22) }
{ population: NumberLong(9000000000) }
</pre>

<b>3. Double</b><br>
• 64-bit floating point<br>
Example:
<pre>
{ salary: 55000.75 }
</pre>

<b>4. Decimal128</b><br>
• High precision decimal (financial systems)<br>
• Avoids floating-point errors<br>
Example:
<pre>
{ balance: NumberDecimal("10500.45") }
</pre>

Used in Banking & Financial applications.<br><br>

<hr>

<b>2️⃣ Boolean</b><br>

<pre>
{ isActive: true }
</pre>

Used for status flags.<br><br>

<hr>

<b>3️⃣ Date</b><br>

• Stores UTC datetime<br>
• 64-bit integer internally<br>

<pre>
{ createdAt: new Date() }
</pre>

Used in TTL indexes, analytics, and time-series collections.<br><br>

<hr>

<b>4️⃣ ObjectId</b><br>

• Default _id type<br>
• 12-byte unique identifier<br>
• Contains timestamp, machine id, process id, counter<br>

<pre>
{ _id: ObjectId("507f1f77bcf86cd799439011") }
</pre>

Automatically generated if not provided.<br><br>

<hr>

<b>5️⃣ Array</b><br>

• Stores multiple values<br>
• Supports multi-key indexing<br>

<pre>
{ skills: ["Java", "Spring Boot", "MongoDB"] }
</pre>

Arrays can store mixed data types.<br><br>

<hr>

<b>6️⃣ Embedded Document (Object)</b><br>

• Nested JSON object<br>

<pre>
{
  address: {
    city: "Pune",
    state: "MH",
    pin: 411001
  }
}
</pre>

Used for data denormalization.<br><br>

<hr>

<b>7️⃣ Null</b><br>

<pre>
{ middleName: null }
</pre>

Indicates field exists but has no value.<br><br>

<hr>

<b>8️⃣ Special / Advanced BSON Types</b><br><br>

<b>1. Binary Data</b><br>
• Store files, encrypted data<br>

<b>2. Timestamp</b><br>
• Internal MongoDB replication use<br>
• Different from Date<br>

<b>3. Regular Expression</b><br>
<pre>
{ name: /pratiksha/i }
</pre>

<b>4. MinKey / MaxKey</b><br>
• Used in internal comparisons & sharding<br>

<b>5. JavaScript</b><br>
• Store JS functions (rarely used in production)<br>

<b>6. Undefined (Deprecated)</b><br>

<hr>

<b>9️⃣ Type Comparison Order (Important for Indexing)</b><br><br>

MongoDB sorts types in this order:

MinKey → Null → Numbers → String → Object → Array → Binary →
ObjectId → Boolean → Date → Timestamp → Regex → MaxKey

Understanding this is important for compound indexes
and range queries.<br><br>

<hr>

<b>🔟 Production Best Practices</b><br><br>

✔ Use Decimal128 for financial amounts  
✔ Avoid mixing data types in same field  
✔ Use Date type for time-based queries  
✔ Use embedded documents for one-to-few relationships  
✔ Avoid storing large binary data directly (use GridFS)  
✔ Define schema validation using $jsonSchema  

<hr>

<b>Enterprise Insight:</b><br><br>

Correct BSON type selection directly impacts:
• Query performance  
• Index efficiency  
• Aggregation accuracy  
• Storage optimization  
• Sharding behavior  

Improper type usage (e.g., storing numbers as strings)
can break indexes and degrade performance significantly.
`
  }
]
},
{
  id: "insert-document",
  name: "Insert Document in MongoDB",
   beginner: [
    {
      content: `
<b>Insert Document in MongoDB (Beginner Level)</b><br><br>

In MongoDB, inserting a document means adding data into a collection.<br>
A document is stored in JSON-like format inside a collection.<br><br>

<hr>

<b>1. Insert One Document</b><br><br>

Use <b>insertOne()</b> to insert a single document.<br><br>

<pre>
use StudentDB

db.students.insertOne({
  name: "Pratiksha",
  age: 22,
  course: "MongoDB"
})
</pre>

If successful, MongoDB returns:<br>

<pre>
{
  acknowledged: true,
  insertedId: ObjectId("...")
}
</pre>

<hr>

<b>2. Insert Multiple Documents</b><br><br>

Use <b>insertMany()</b> to insert multiple documents at once.<br><br>

<pre>
db.students.insertMany([
  { name: "Amit", age: 23 },
  { name: "Sneha", age: 21 }
])
</pre>

<hr>

<b>Important Points</b><br><br>

• If the collection does not exist, MongoDB creates it automatically.<br>
• Each document automatically gets a unique <b>_id</b> field.<br>
• Data is stored in BSON format.<br><br>

<hr>

<b>Verify Inserted Data</b><br><br>

To check inserted documents:<br>

<pre>
db.students.find()
</pre>

<hr>

<b>Summary</b><br><br>

• insertOne() → Insert single document<br>
• insertMany() → Insert multiple documents<br>
• find() → View documents<br><br>
`
    }
  ],
  intermediate: [
    {
      content: `
<b>Insert Document in MongoDB</b><br><br>

In MongoDB, inserting a document means adding new data into a collection.
MongoDB provides multiple methods to insert documents.<br><br>

<hr>

<b>1. insertOne()</b><br><br>

Used to insert a single document into a collection.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.insertOne(document)
</pre>

<b>Example:</b><br>
<pre>
db.students.insertOne({
  name: "Pratiksha",
  age: 22,
  city: "Pune"
})
</pre>

MongoDB automatically generates a unique <b>_id</b> if not provided.<br><br>

<hr>

<b>2. insertMany()</b><br><br>

Used to insert multiple documents at once.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.insertMany([document1, document2, ...])
</pre>

<b>Example:</b><br>
<pre>
db.students.insertMany([
  { name: "Rahul", age: 23 },
  { name: "Sneha", age: 21 }
])
</pre>

<hr>

<b>3. Insert Document with Custom _id</b><br><br>

You can manually provide the _id field.<br><br>

<b>Example:</b><br>
<pre>
db.students.insertOne({
  _id: 101,
  name: "Amit",
  age: 24
})
</pre>

<hr>

<b>Important Points</b><br><br>

• If collection does not exist, MongoDB creates it automatically.<br>
• _id field must be unique.<br>
• insertOne() is preferred for single document insertion.<br>
• insertMany() improves performance for bulk insertion.<br>
• If duplicate _id is inserted, MongoDB throws an error.<br><br>

<hr>

<b>Real Example Flow</b><br><br>

Step 1: Switch Database<br>
<pre>
use CollegeDB
</pre>

Step 2: Insert Document<br>
<pre>
db.students.insertOne({ name: "Kiran", age: 22 })
</pre>

Step 3: Verify<br>
<pre>
db.students.find()
</pre>
`
    }
  ],
  expert: [
  {
    content: `
<b>Insert Document in MongoDB (Expert Level Guide)</b><br><br>

In MongoDB, inserting a document means adding a BSON document
into a collection. Insert operations are atomic at the document level
and support write concern, transactions, bulk operations,
and validation rules in production systems.<br><br>

<hr>

<b>1️⃣ Basic Insert Operations</b><br><br>

<b>insertOne()</b> – Insert a single document:

<pre>
db.customers.insertOne({
  name: "Pratiksha",
  email: "pratiksha@gmail.com",
  balance: NumberDecimal("10500.50"),
  accountType: "Savings",
  createdAt: new Date()
})
</pre>

Return Example:
<pre>
{
  acknowledged: true,
  insertedId: ObjectId("65f1ab2345abcd1234567890")
}
</pre>

• acknowledged → Write confirmation  
• insertedId → Auto-generated ObjectId<br><br>

<hr>

<b>2️⃣ Insert Multiple Documents</b><br><br>

<b>insertMany()</b> – Insert multiple documents:

<pre>
db.customers.insertMany([
  { name: "Amit", balance: 5000 },
  { name: "Riya", balance: 8000 },
  { name: "John", balance: 12000 }
])
</pre>

Options:
<pre>
db.customers.insertMany(documents, { ordered: false })
</pre>

• ordered: true (default) → Stops on first error  
• ordered: false → Continues inserting even if one fails<br><br>

<hr>

<b>3️⃣ Insert with Write Concern (Production Control)</b><br><br>

<pre>
db.customers.insertOne(
  { name: "Secure User", balance: 1000 },
  { writeConcern: { w: "majority", j: true, wtimeout: 5000 } }
)
</pre>

Write Concern Options:
• w: "majority" → Wait for replication  
• j: true → Wait for journal commit  
• wtimeout → Timeout in ms  

Used in Banking & Financial systems.<br><br>

<hr>

<b>4️⃣ Insert in Transactions (MongoDB 4.0+)</b><br><br>

<pre>
session = db.getMongo().startSession()
session.startTransaction()

try {
  session.getDatabase("BankingDB").accounts.insertOne({
    accountNo: 12345,
    balance: 20000
  })

  session.commitTransaction()
} catch (e) {
  session.abortTransaction()
}
</pre>

Used when:
• Multiple related inserts must succeed together  
• Financial transfers  
• Distributed systems<br><br>

<hr>

<b>5️⃣ Bulk Insert Operations</b><br><br>

<pre>
db.customers.bulkWrite([
  {
    insertOne: {
      document: { name: "Bulk1", balance: 100 }
    }
  },
  {
    insertOne: {
      document: { name: "Bulk2", balance: 200 }
    }
  }
])
</pre>

Advantages:
• Reduced network overhead  
• Better performance for large datasets  
• Used in ETL pipelines<br><br>

<hr>

<b>6️⃣ Schema Validation Impact</b><br><br>

If collection has validation rules:

• Invalid document → Rejected  
• validationAction: "error" → Insert fails  
• validationAction: "warn" → Insert succeeds with warning<br><br>

<hr>

<b>7️⃣ Performance & Index Considerations</b><br><br>

✔ Insert is atomic per document  
✔ Indexes are updated during insert  
✔ More indexes = slower insert performance  
✔ Use batching for high-volume inserts  
✔ Avoid very large documents (>16MB limit)  
✔ Monitor using db.collection.stats()<br><br>

<hr>

<b>8️⃣ Sharded Cluster Behavior</b><br><br>

• Document routed based on shard key  
• Proper shard key selection prevents hotspotting  
• Insert without shard key (if required) will fail  

<hr>

<b>9️⃣ TTL & Expiring Documents</b><br><br>

If TTL index exists:

<pre>
db.sessions.createIndex({ createdAt: 1 }, { expireAfterSeconds: 3600 })
</pre>

Documents automatically deleted after 1 hour.<br><br>

<hr>

<b>🔟 Enterprise Best Practices</b><br><br>

✔ Use Decimal128 for monetary values  
✔ Use insertMany() with batching for performance  
✔ Always define proper write concern  
✔ Avoid auto-creating collections in production  
✔ Validate schema before inserting  
✔ Log and monitor failed insert operations  

<hr>

<b>Expert Insight:</b><br><br>

Insert performance depends on:
• Index count  
• Write concern level  
• Storage engine (WiredTiger)  
• Shard key distribution  
• Document size  

Optimizing insert strategy is critical in
high-throughput systems like banking,
e-commerce, and real-time analytics.
`
  }
]
},

{
  id: "query-document",
  name: "Query Documents in MongoDB",
   beginner: [
    {
      content: `
<b>Query Documents in MongoDB (Beginner Level)</b><br><br>

Querying documents means retrieving (reading) data from a collection.<br>
MongoDB uses the <b>find()</b> method to fetch documents.<br><br>

<hr>

<b>1. Find All Documents</b><br><br>

To display all documents in a collection:<br>

<pre>
use StudentDB

db.students.find()
</pre>

<hr>

<b>2. Find One Document</b><br><br>

To get only one document:<br>

<pre>
db.students.findOne()
</pre>

<hr>

<b>3. Find with Condition</b><br><br>

You can filter documents using conditions.<br><br>

Example: Find student with age 22<br>

<pre>
db.students.find({ age: 22 })
</pre>

<hr>

<b>4. Find with Multiple Conditions</b><br><br>

Example:<br>

<pre>
db.students.find({ name: "Pratiksha", age: 22 })
</pre>

<hr>

<b>5. Show Specific Fields (Projection)</b><br><br>

Example: Show only name field<br>

<pre>
db.students.find({}, { name: 1, _id: 0 })
</pre>

• 1 → Show field<br>
• 0 → Hide field<br><br>

<hr>

<b>Important Points</b><br><br>

• find() returns all matching documents.<br>
• findOne() returns only one document.<br>
• Conditions are written inside {} brackets.<br><br>

<hr>

<b>Summary</b><br><br>

• find() → Retrieve multiple documents<br>
• findOne() → Retrieve single document<br>
• {} → Used for conditions<br>
• Projection → Select specific fields<br><br>
`
    }
  ],
  intermediate: [
    {
      content: `
<b>Query Documents in MongoDB</b><br><br>

Querying documents means retrieving data from a collection.
MongoDB provides flexible query methods to filter and display documents.<br><br>

<hr>

<b>1. find()</b><br><br>

Used to retrieve multiple documents from a collection.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.find()
</pre>

<b>Example:</b><br>
<pre>
db.students.find()
</pre>

Returns all documents inside the collection.<br><br>

<hr>

<b>2. findOne()</b><br><br>

Used to retrieve only one document.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.findOne()
</pre>

<b>Example:</b><br>
<pre>
db.students.findOne()
</pre>

<hr>

<b>3. Query with Condition</b><br><br>

You can filter documents by specifying conditions.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.find({ condition })
</pre>

<b>Example:</b><br>
<pre>
db.students.find({ age: 22 })
</pre>

<hr>

<b>4. Using Comparison Operators</b><br><br>

<b>$gt</b> → Greater than<br>
<b>$lt</b> → Less than<br>
<b>$gte</b> → Greater than or equal<br>
<b>$lte</b> → Less than or equal<br>
<b>$ne</b> → Not equal<br><br>

<b>Example:</b><br>
<pre>
db.students.find({ age: { $gt: 20 } })
</pre>

<hr>

<b>5. Projection (Select Specific Fields)</b><br><br>

Used to display only selected fields.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.find(
  { condition },
  { field1: 1, field2: 1 }
)
</pre>

<b>Example:</b><br>
<pre>
db.students.find(
  { age: 22 },
  { name: 1, _id: 0 }
)
</pre>

<hr>

<b>6. Pretty Format</b><br><br>

To display result in readable format:<br>

<pre>
db.students.find().pretty()
</pre>

<hr>

<b>Important Points</b><br><br>

• find() returns a cursor.<br>
• Queries can be combined with operators.<br>
• MongoDB queries are case-sensitive.<br>
• Indexing improves query performance.<br><br>

<hr>

<b>Real Example Flow</b><br><br>

Step 1: Switch Database<br>
<pre>
use CollegeDB
</pre>

Step 2: Query Data<br>
<pre>
db.students.find({ city: "Pune" })
</pre>

Step 3: Use Projection<br>
<pre>
db.students.find({ city: "Pune" }, { name: 1 })
</pre>
`
    }
  ],
  expert: [
  {
    content: `
<b>Query Documents in MongoDB (Expert Level Guide)</b><br><br>

Querying documents in MongoDB is performed using the find() method
and powerful query operators. MongoDB uses BSON-based querying,
supports indexing, aggregation pipelines, text search, geospatial queries,
and advanced filtering mechanisms.<br><br>

<hr>

<b>1️⃣ Basic Query Syntax</b><br><br>

<pre>
db.collection.find(filter, projection)
</pre>

• filter → Conditions to match documents  
• projection → Fields to include/exclude  

Example:
<pre>
db.customers.find({ accountType: "Savings" })
</pre>

<hr>

<b>2️⃣ Comparison Operators</b><br><br>

<pre>
db.customers.find({ balance: { $gt: 5000 } })
</pre>

Common Operators:
• $eq  → Equal  
• $ne  → Not equal  
• $gt  → Greater than  
• $gte → Greater than equal  
• $lt  → Less than  
• $lte → Less than equal  
• $in  → Match any value in array  
• $nin → Not in array  

<hr>

<b>3️⃣ Logical Operators</b><br><br>

<pre>
db.customers.find({
  $and: [
    { balance: { $gt: 5000 } },
    { accountType: "Savings" }
  ]
})
</pre>

Operators:
• $and  
• $or  
• $not  
• $nor  

<hr>

<b>4️⃣ Projection (Field Selection)</b><br><br>

Include specific fields:

<pre>
db.customers.find(
  { balance: { $gt: 5000 } },
  { name: 1, balance: 1, _id: 0 }
)
</pre>

• 1 → Include field  
• 0 → Exclude field  

<hr>

<b>5️⃣ Query Embedded Documents</b><br><br>

<pre>
db.users.find({ "address.city": "Pune" })
</pre>

<hr>

<b>6️⃣ Query Arrays</b><br><br>

Match array value:

<pre>
db.students.find({ skills: "MongoDB" })
</pre>

Using $elemMatch:

<pre>
db.students.find({
  scores: { $elemMatch: { $gt: 80 } }
})
</pre>

<hr>

<b>7️⃣ Sorting, Limiting & Skipping</b><br><br>

<pre>
db.customers.find()
  .sort({ balance: -1 })
  .limit(5)
  .skip(10)
</pre>

• 1  → Ascending  
• -1 → Descending  

Used in pagination systems.<br><br>

<hr>

<b>8️⃣ Index & Query Optimization</b><br><br>

Check query execution plan:

<pre>
db.customers.find({ balance: { $gt: 5000 } }).explain("executionStats")
</pre>

Important Metrics:
• COLLSCAN → Full collection scan (slow)  
• IXSCAN → Index scan (fast)  
• nReturned → Documents returned  
• executionTimeMillis → Query time  

Best Practice:
✔ Create index on frequently queried fields  
✔ Avoid querying without indexes in large datasets  

<hr>

<b>9️⃣ Text Search Query</b><br><br>

Create text index:

<pre>
db.products.createIndex({ description: "text" })
</pre>

Query:

<pre>
db.products.find({ $text: { $search: "laptop" } })
</pre>

<hr>

<b>🔟 Aggregation-Based Query (Advanced)</b><br><br>

<pre>
db.transactions.aggregate([
  { $match: { amount: { $gt: 1000 } } },
  { $group: { _id: "$accountType", total: { $sum: "$amount" } } },
  { $sort: { total: -1 } }
])
</pre>

Used for analytics and reporting.<br><br>

<hr>

<b>1️⃣1️⃣ Geospatial Query</b><br><br>

<pre>
db.places.find({
  location: {
    $near: {
      $geometry: {
        type: "Point",
        coordinates: [73.8567, 18.5204]
      },
      $maxDistance: 5000
    }
  }
})
</pre>

Requires 2dsphere index.<br><br>

<hr>

<b>1️⃣2️⃣ Query in Transactions</b><br><br>

Queries inside transactions provide consistent reads
in multi-document ACID operations.<br><br>

<hr>

<b>Performance Best Practices</b><br><br>

✔ Always index frequently queried fields  
✔ Use projection to reduce network load  
✔ Avoid large result sets without limit()  
✔ Use explain() for optimization  
✔ Prefer aggregation for complex analytics  
✔ Use covered queries for maximum performance  

<hr>

<b>Expert Insight:</b><br><br>

Query performance depends on:
• Proper index strategy  
• Selectivity of query  
• Data type consistency  
• Shard key design  
• Read preference settings  

Poor query design can lead to:
• Full collection scans  
• Memory pressure  
• Slow response times  
• Cluster imbalance  

Optimized querying is critical in high-volume
banking, e-commerce, and analytics systems.
`
  }
]
},
{
  id: "update-document",
  name: "Update Documents in MongoDB",
  beginner: [
    {
      content: `
<b>Update Documents in MongoDB (Beginner Level)</b><br><br>

Updating documents means modifying existing data inside a collection.<br>
MongoDB provides update methods to change specific fields in documents.<br><br>

<hr>

<b>1. updateOne()</b><br><br>

Used to update only one matching document.<br><br>

Example:<br>

<pre>
use StudentDB

db.students.updateOne(
  { name: "Pratiksha" },
  { $set: { age: 23 } }
)
</pre>

• First {} → Condition<br>
• $set → Updates the field value<br><br>

<hr>

<b>2. updateMany()</b><br><br>

Used to update multiple matching documents.<br><br>

Example:<br>

<pre>
db.students.updateMany(
  { course: "MongoDB" },
  { $set: { status: "Completed" } }
)
</pre>

<hr>

<b>3. Replace Entire Document</b><br><br>

You can replace the whole document using replaceOne().<br><br>

Example:<br>

<pre>
db.students.replaceOne(
  { name: "Amit" },
  { name: "Amit", age: 24, course: "Java" }
)
</pre>

⚠ This replaces the full document except _id.<br><br>

<hr>

<b>Common Update Operators</b><br><br>

• $set → Update specific field<br>
• $inc → Increase numeric value<br>
• $unset → Remove a field<br><br>

Example ($inc):<br>

<pre>
db.students.updateOne(
  { name: "Pratiksha" },
  { $inc: { age: 1 } }
)
</pre>

<hr>

<b>Verify Update</b><br><br>

<pre>
db.students.find()
</pre>

<hr>

<b>Summary</b><br><br>

• updateOne() → Update single document<br>
• updateMany() → Update multiple documents<br>
• $set → Modify field<br>
• $inc → Increment value<br><br>
`
    }
  ],
  intermediate: [
    {
      content: `
<b>Update Documents in MongoDB</b><br><br>

Updating a document means modifying existing data in a collection.
MongoDB provides multiple methods to update documents.<br><br>

<hr>

<b>1. updateOne()</b><br><br>

Used to update a single document that matches the condition.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.updateOne(
  { filter },
  { $set: { field: value } }
)
</pre>

<b>Example:</b><br>
<pre>
db.students.updateOne(
  { name: "Pratiksha" },
  { $set: { age: 23 } }
)
</pre>

<hr>

<b>2. updateMany()</b><br><br>

Used to update multiple documents that match the condition.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.updateMany(
  { filter },
  { $set: { field: value } }
)
</pre>

<b>Example:</b><br>
<pre>
db.students.updateMany(
  { city: "Pune" },
  { $set: { city: "Mumbai" } }
)
</pre>

<hr>

<b>3. replaceOne()</b><br><br>

Replaces the entire document except the _id field.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.replaceOne(
  { filter },
  { newDocument }
)
</pre>

<b>Example:</b><br>
<pre>
db.students.replaceOne(
  { name: "Rahul" },
  { name: "Rahul", age: 25, city: "Delhi" }
)
</pre>

<hr>

<b>4. Common Update Operators</b><br><br>

<b>$set</b> → Updates specific field<br>
<b>$inc</b> → Increments numeric value<br>
<b>$unset</b> → Removes field<br>
<b>$push</b> → Adds value to array<br>
<b>$pull</b> → Removes value from array<br><br>

<b>Example ($inc):</b><br>
<pre>
db.students.updateOne(
  { name: "Pratiksha" },
  { $inc: { age: 1 } }
)
</pre>

<hr>

<b>Important Points</b><br><br>

• Always use update operators like $set to modify fields.<br>
• Without operators, the entire document may be replaced.<br>
• _id field cannot be modified.<br>
• updateOne() modifies only the first matched document.<br>
• updateMany() modifies all matched documents.<br><br>

<hr>

<b>Real Example Flow</b><br><br>

Step 1: Switch Database<br>
<pre>
use CollegeDB
</pre>

Step 2: Update Document<br>
<pre>
db.students.updateOne(
  { name: "Kiran" },
  { $set: { age: 24 } }
)
</pre>

Step 3: Verify<br>
<pre>
db.students.find({ name: "Kiran" })
</pre>
`
    }
  ],
  expert: [
  {
    content: `
<b>Update Documents in MongoDB (Expert Level Guide)</b><br><br>

Updating documents in MongoDB modifies existing BSON documents
using atomic update operators. Updates are atomic at the document level
and support bulk operations, transactions, array modifiers,
upserts, and aggregation pipeline updates.<br><br>

<hr>

<b>1️⃣ Basic Update Syntax</b><br><br>

<pre>
db.collection.updateOne(filter, update, options)
db.collection.updateMany(filter, update, options)
</pre>

• filter  → Select documents  
• update  → Update operators  
• options → upsert, writeConcern, arrayFilters  

<hr>

<b>2️⃣ Update Single Document</b><br><br>

<pre>
db.customers.updateOne(
  { email: "pratiksha@gmail.com" },
  { $set: { balance: 15000 } }
)
</pre>

Return Example:
<pre>
{
  acknowledged: true,
  matchedCount: 1,
  modifiedCount: 1
}
</pre>

<hr>

<b>3️⃣ Update Multiple Documents</b><br><br>

<pre>
db.customers.updateMany(
  { accountType: "Savings" },
  { $inc: { balance: 500 } }
)
</pre>

$inc → Increment numeric field<br><br>

<hr>

<b>4️⃣ Common Update Operators</b><br><br>

• $set → Set field value  
• $unset → Remove field  
• $inc → Increment value  
• $mul → Multiply value  
• $rename → Rename field  
• $min / $max → Conditional update  
• $currentDate → Update with current date  

Example:

<pre>
db.customers.updateOne(
  { name: "Amit" },
  {
    $set: { status: "Active" },
    $currentDate: { updatedAt: true }
  }
)
</pre>

<hr>

<b>5️⃣ Array Updates (Advanced)</b><br><br>

Push into array:

<pre>
db.students.updateOne(
  { name: "Pratiksha" },
  { $push: { skills: "MongoDB" } }
)
</pre>

Add unique value:

<pre>
{ $addToSet: { skills: "Java" } }
</pre>

Update specific array element:

<pre>
db.orders.updateOne(
  { "items.productId": 101 },
  { $set: { "items.$.price": 1200 } }
)
</pre>

Using arrayFilters:

<pre>
db.orders.updateMany(
  {},
  { $set: { "items.$[elem].discount": 10 } },
  { arrayFilters: [ { "elem.price": { $gt: 1000 } } ] }
)
</pre>

<hr>

<b>6️⃣ Upsert (Insert if Not Exists)</b><br><br>

<pre>
db.customers.updateOne(
  { email: "new@gmail.com" },
  { $set: { name: "New User", balance: 1000 } },
  { upsert: true }
)
</pre>

If no document matches → new document inserted.<br><br>

<hr>

<b>7️⃣ Aggregation Pipeline Update (MongoDB 4.2+)</b><br><br>

<pre>
db.accounts.updateOne(
  { accountNo: 12345 },
  [
    { $set: { balance: { $add: ["$balance", 500] } } }
  ]
)
</pre>

Used for:
• Conditional transformations  
• Complex field computations  
• Data migrations<br><br>

<hr>

<b>8️⃣ Update in Transactions</b><br><br>

<pre>
session = db.getMongo().startSession()
session.startTransaction()

try {
  session.getDatabase("BankingDB").accounts.updateOne(
    { accountNo: 101 },
    { $inc: { balance: -500 } }
  )

  session.getDatabase("BankingDB").accounts.updateOne(
    { accountNo: 202 },
    { $inc: { balance: 500 } }
  )

  session.commitTransaction()
} catch (e) {
  session.abortTransaction()
}
</pre>

Ensures ACID compliance in financial transfers.<br><br>

<hr>

<b>9️⃣ Write Concern & Performance</b><br><br>

<pre>
db.customers.updateOne(
  { name: "SecureUser" },
  { $set: { status: "Verified" } },
  { writeConcern: { w: "majority", j: true } }
)
</pre>

Performance Notes:
• Updates modify document in place (if size unchanged)  
• Large document growth may trigger document relocation  
• More indexes = slower updates  
• Atomic at document level only  

<hr>

<b>🔟 Update vs Replace</b><br><br>

Replace entire document:

<pre>
db.customers.replaceOne(
  { email: "amit@gmail.com" },
  { name: "Amit", balance: 20000 }
)
</pre>

replaceOne() removes all existing fields except _id.<br><br>

<hr>

<b>1️⃣1️⃣ Index & Query Plan Analysis</b><br><br>

<pre>
db.customers.updateOne(
  { email: "amit@gmail.com" },
  { $set: { status: "Active" } }
).explain("executionStats")
</pre>

Ensure update filter uses indexed field to avoid COLLSCAN.<br><br>

<hr>

<b>Enterprise Best Practices</b><br><br>

✔ Always index update filter fields  
✔ Use $set instead of replacing full document  
✔ Avoid unbounded array growth  
✔ Use transactions for multi-document updates  
✔ Monitor update latency with profiler  
✔ Use schema validation to protect structure  

<hr>

<b>Expert Insight:</b><br><br>

Update performance depends on:
• Index design  
• Document size growth  
• Shard key distribution  
• Write concern level  
• Storage engine behavior  

Poor update strategy can cause:
• Document fragmentation  
• High disk I/O  
• Lock contention  
• Cluster imbalance  

Careful update design is critical in high-scale
banking, analytics, and enterprise applications.
`
  }
]
},
{
  id: "delete-document",
  name: "Delete Documents in MongoDB",
  beginner: [
  {
    content: `
<b>Delete Documents in MongoDB</b><br><br>

Deleting a document means removing data from a collection.
MongoDB allows you to delete one or many documents using simple methods.<br><br>

<hr>

<b>1. deleteOne()</b><br><br>

Used to delete only one document that matches the condition.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.deleteOne({ condition })
</pre>

<b>Example:</b><br>
<pre>
db.students.deleteOne({ name: "Pratiksha" })
</pre>

This will delete only the first matching document.<br><br>

<hr>

<b>2. deleteMany()</b><br><br>

Used to delete multiple documents that match the condition.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.deleteMany({ condition })
</pre>

<b>Example:</b><br>
<pre>
db.students.deleteMany({ city: "Pune" })
</pre>

All documents where city is "Pune" will be deleted.<br><br>

<hr>

<b>3. Delete All Documents</b><br><br>

To remove all documents from a collection (but keep the collection):<br>

<pre>
db.students.deleteMany({})
</pre>

<hr>

<b>Important Points</b><br><br>

• deleteOne() deletes only one document.<br>
• deleteMany() deletes all matching documents.<br>
• Deletion is permanent.<br>
• Always check data using find() before deleting.<br><br>

<hr>

<b>Simple Example Flow</b><br><br>

Step 1: Use Database<br>
<pre>
use CollegeDB
</pre>

Step 2: Delete Document<br>
<pre>
db.students.deleteOne({ name: "Kiran" })
</pre>

Step 3: Check Remaining Data<br>
<pre>
db.students.find()
</pre>
`
  }
],
  intermediate: [
    {
      content: `
<b>Delete Documents in MongoDB</b><br><br>

Deleting a document means removing existing data from a collection.
MongoDB provides methods to delete single or multiple documents based on conditions.<br><br>

<hr>

<b>1. deleteOne()</b><br><br>

Used to delete only one document that matches the condition.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.deleteOne({ filter })
</pre>

<b>Example:</b><br>
<pre>
db.students.deleteOne({ name: "Pratiksha" })
</pre>

Only the first matched document will be deleted.<br><br>

<hr>

<b>2. deleteMany()</b><br><br>

Used to delete multiple documents that match the condition.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.deleteMany({ filter })
</pre>

<b>Example:</b><br>
<pre>
db.students.deleteMany({ city: "Pune" })
</pre>

All documents matching the condition will be removed.<br><br>

<hr>

<b>3. Delete All Documents</b><br><br>

To remove all documents from a collection (but keep the collection):<br>

<pre>
db.students.deleteMany({})
</pre>

<hr>

<b>Important Points</b><br><br>

• Deletion is permanent and cannot be undone.<br>
• deleteOne() removes only first matched document.<br>
• deleteMany() removes all matched documents.<br>
• Use filter carefully to avoid accidental data loss.<br>
• To remove entire collection, use drop() instead.<br><br>

<hr>

<b>Real Example Flow</b><br><br>

Step 1: Switch Database<br>
<pre>
use CollegeDB
</pre>

Step 2: Delete Document<br>
<pre>
db.students.deleteOne({ name: "Kiran" })
</pre>

Step 3: Verify<br>
<pre>
db.students.find()
</pre>
`
    }
  ],
  expert: [
  {
    content: `
<b>Delete Documents in MongoDB (Expert Level Guide)</b><br><br>

Deleting documents in MongoDB removes selected BSON documents
from a collection. Delete operations are atomic at the document level,
support write concern, transactions, bulk operations,
and behave differently in replica sets and sharded clusters.<br><br>

<hr>

<b>1️⃣ Basic Delete Syntax</b><br><br>

<pre>
db.collection.deleteOne(filter, options)
db.collection.deleteMany(filter, options)
</pre>

• filter  → Select documents to remove  
• options → writeConcern, collation, hint  

<hr>

<b>2️⃣ Delete Single Document</b><br><br>

<pre>
db.customers.deleteOne({ email: "amit@gmail.com" })
</pre>

Return Example:
<pre>
{
  acknowledged: true,
  deletedCount: 1
}
</pre>

Deletes the first matching document.<br><br>

<hr>

<b>3️⃣ Delete Multiple Documents</b><br><br>

<pre>
db.customers.deleteMany({ accountType: "Inactive" })
</pre>

Deletes all documents matching the filter.<br><br>

<hr>

<b>4️⃣ Delete All Documents (Careful ⚠️)</b><br><br>

<pre>
db.customers.deleteMany({})
</pre>

• Removes all documents  
• Keeps collection and indexes intact  

For full removal including indexes → use drop()<br><br>

<hr>

<b>5️⃣ Delete with Write Concern</b><br><br>

<pre>
db.customers.deleteOne(
  { email: "secure@gmail.com" },
  { writeConcern: { w: "majority", j: true, wtimeout: 5000 } }
)
</pre>

Used in financial or critical systems
to ensure deletion is replicated.<br><br>

<hr>

<b>6️⃣ Delete with Collation</b><br><br>

<pre>
db.users.deleteMany(
  { name: "pratiksha" },
  { collation: { locale: "en", strength: 2 } }
)
</pre>

• Case-insensitive delete  

<hr>

<b>7️⃣ Bulk Delete Operations</b><br><br>

<pre>
db.customers.bulkWrite([
  { deleteOne: { filter: { balance: { $lt: 0 } } } },
  { deleteMany: { filter: { status: "Closed" } } }
])
</pre>

Used in:
• ETL processes  
• Data cleanup jobs  
• Migration scripts  

<hr>

<b>8️⃣ Delete in Transactions (MongoDB 4.0+)</b><br><br>

<pre>
session = db.getMongo().startSession()
session.startTransaction()

try {
  session.getDatabase("BankingDB").accounts.deleteOne({ accountNo: 101 })
  session.getDatabase("BankingDB").transactions.deleteMany({ accountNo: 101 })

  session.commitTransaction()
} catch (e) {
  session.abortTransaction()
}
</pre>

Ensures ACID compliance across multiple collections.<br><br>

<hr>

<b>9️⃣ TTL (Automatic Deletion)</b><br><br>

Create TTL index:

<pre>
db.sessions.createIndex(
  { createdAt: 1 },
  { expireAfterSeconds: 3600 }
)
</pre>

Documents automatically deleted after 1 hour.<br><br>

<hr>

<b>🔟 Performance & Index Considerations</b><br><br>

✔ Delete filter should use indexed fields  
✔ Without index → COLLSCAN (slow)  
✔ Large deletes may impact performance  
✔ Consider batch deletes for massive datasets  
✔ Monitor using explain("executionStats")  

Example:

<pre>
db.customers.deleteMany({ balance: { $lt: 1000 } })
  .explain("executionStats")
</pre>

<hr>

<b>1️⃣1️⃣ Behavior in Distributed Systems</b><br><br>

Replica Set:
• Delete operation logged in oplog  
• Replicated to secondary nodes  

Sharded Cluster:
• Routed based on shard key  
• Efficient if shard key included in filter  
• Otherwise scatter-gather operation  

<hr>

<b>1️⃣2️⃣ deleteMany() vs drop()</b><br><br>

deleteMany({}):
• Removes documents only  
• Keeps indexes & structure  

drop():
• Removes entire collection  
• Deletes indexes & metadata  
• Frees storage completely  

<hr>

<b>Enterprise Best Practices</b><br><br>

✔ Always verify filter before delete  
✔ Avoid deleting without condition in production  
✔ Use transactions for related deletes  
✔ Use TTL for automatic cleanup  
✔ Archive important data before deletion  
✔ Restrict delete privileges via RBAC  

<hr>

<b>Expert Insight:</b><br><br>

Delete performance depends on:
• Index usage  
• Document size  
• Shard key design  
• Write concern level  
• Volume of data  

Improper delete strategy can cause:
• Lock contention  
• Replication lag  
• Disk fragmentation  
• Performance degradation  

Careful deletion planning is critical in
banking, healthcare, and high-scale enterprise systems.
`
  }
]
},

{
  id: "projection",
  name: "Projection in MongoDB",
  beginner: [
  {
    content: `
<b>Projection in MongoDB</b><br><br>

Projection is used to select specific fields from documents.
It helps to display only required data instead of the full document.<br><br>

<hr>

<b>1. Basic Syntax</b><br><br>

<pre>
db.collectionName.find({ condition }, { field: 1 })
</pre>

• 1 → Show the field<br>
• 0 → Hide the field<br><br>

<hr>

<b>2. Show Only Specific Fields</b><br><br>

<pre>
db.students.find({}, { name: 1, age: 1 })
</pre>

This will display only name and age fields.<br><br>

<hr>

<b>3. Hide Specific Field</b><br><br>

<pre>
db.students.find({}, { city: 0 })
</pre>

This will hide the city field and show other fields.<br><br>

<hr>

<b>4. Remove _id Field</b><br><br>

By default, MongoDB always shows _id field.
To hide it:<br>

<pre>
db.students.find({}, { name: 1, age: 1, _id: 0 })
</pre>

<hr>

<b>Important Points</b><br><br>

• Projection works with find() method.<br>
• Cannot mix 1 and 0 together (except _id).<br>
• Helps improve performance by returning less data.<br><br>

<hr>

<b>Example Flow</b><br><br>

Step 1: Use Database<br>
<pre>
use CollegeDB
</pre>

Step 2: View Only Name Field<br>
<pre>
db.students.find({}, { name: 1, _id: 0 })
</pre>
`
  }
],
  intermediate: [
    {
      content: `
<b>Projection in MongoDB</b><br><br>

Projection is used to select specific fields from documents while querying data.
It helps in reducing the amount of data returned and improves performance.<br><br>

By default, MongoDB returns all fields of a document.<br>
Using projection, we can include or exclude specific fields.<br><br>

<hr>

<b>1. Include Specific Fields</b><br><br>

To display only selected fields, use 1 to include fields.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.find(
  { condition },
  { field1: 1, field2: 1 }
)
</pre>

<b>Example:</b><br>
<pre>
db.students.find(
  { age: 22 },
  { name: 1, city: 1 }
)
</pre>

Only name and city fields will be displayed (along with _id by default).<br><br>

<hr>

<b>2. Exclude Specific Fields</b><br><br>

Use 0 to exclude fields.<br><br>

<b>Example:</b><br>
<pre>
db.students.find(
  {},
  { age: 0 }
)
</pre>

All fields except age will be displayed.<br><br>

<hr>

<b>3. Exclude _id Field</b><br><br>

By default, MongoDB always includes _id.
To remove it from result:<br><br>

<b>Example:</b><br>
<pre>
db.students.find(
  { age: 22 },
  { name: 1, _id: 0 }
)
</pre>

<hr>

<b>Important Rules</b><br><br>

• You cannot mix include (1) and exclude (0) in the same query.<br>
• Exception: _id can be excluded even when including other fields.<br>
• Projection only affects the output, not the stored data.<br>
• Helps improve performance by reducing network load.<br><br>

<hr>

<b>Real Example Flow</b><br><br>

Step 1: Switch Database<br>
<pre>
use CollegeDB
</pre>

Step 2: Query with Projection<br>
<pre>
db.students.find(
  { city: "Pune" },
  { name: 1, _id: 0 }
)
</pre>
`
    }
  ],
  expert: [
  {
    content: `
<b>Projection in MongoDB (Expert Level Guide)</b><br><br>

Projection in MongoDB controls which fields are returned
in query results. It improves performance by reducing
network payload, memory usage, and query execution cost.
Projection is used in both find() queries and aggregation pipelines.<br><br>

<hr>

<b>1️⃣ Basic Projection Syntax</b><br><br>

<pre>
db.collection.find(filter, projection)
</pre>

Example:

<pre>
db.customers.find(
  { accountType: "Savings" },
  { name: 1, balance: 1 }
)
</pre>

• 1 → Include field  
• 0 → Exclude field  

By default, _id is always included unless explicitly excluded.<br><br>

<hr>

<b>2️⃣ Excluding Fields</b><br><br>

<pre>
db.customers.find(
  { balance: { $gt: 5000 } },
  { password: 0, internalNotes: 0 }
)
</pre>

Rule:
You cannot mix inclusion (1) and exclusion (0)
in the same projection except for _id.<br><br>

<hr>

<b>3️⃣ Excluding _id</b><br><br>

<pre>
db.customers.find(
  { balance: { $gt: 5000 } },
  { name: 1, balance: 1, _id: 0 }
)
</pre>

<hr>

<b>4️⃣ Projection on Embedded Documents</b><br><br>

<pre>
db.users.find(
  {},
  { "address.city": 1, name: 1 }
)
</pre>

Returns only selected nested fields.<br><br>

<hr>

<b>5️⃣ Projection with Arrays</b><br><br>

Return specific array element using $slice:

<pre>
db.orders.find(
  {},
  { items: { $slice: 1 } }
)
</pre>

Return last 2 elements:

<pre>
{ items: { $slice: -2 } }
</pre>

Using $elemMatch:

<pre>
db.orders.find(
  {},
  { items: { $elemMatch: { price: { $gt: 1000 } } } }
)
</pre>

<hr>

<b>6️⃣ Projection in Aggregation Pipeline</b><br><br>

Using $project stage:

<pre>
db.transactions.aggregate([
  {
    $project: {
      accountNo: 1,
      amount: 1,
      tax: { $multiply: ["$amount", 0.18] },
      _id: 0
    }
  }
])
</pre>

Advantages:
• Field transformation  
• Computed fields  
• Renaming fields  
• Removing sensitive data<br><br>

<hr>

<b>7️⃣ Covered Queries (High Performance)</b><br><br>

A query is “covered” when:
• All query fields are in index  
• All projected fields are also in index  
• No document fetch required  

Example:

<pre>
db.customers.createIndex({ email: 1, balance: 1 })

db.customers.find(
  { email: "amit@gmail.com" },
  { email: 1, balance: 1, _id: 0 }
).explain("executionStats")
</pre>

If execution shows:
• IXSCAN  
• No FETCH stage  

Then it is a covered query → extremely fast.<br><br>

<hr>

<b>8️⃣ Performance Benefits</b><br><br>

✔ Reduces network bandwidth  
✔ Reduces memory usage  
✔ Improves response time  
✔ Helps create covered queries  
✔ Improves API performance  

<hr>

<b>9️⃣ Security Use Case</b><br><br>

Projection is often used to remove sensitive fields:

<pre>
db.users.find({}, { password: 0, otp: 0 })
</pre>

Critical in:
• Banking systems  
• Authentication services  
• Microservices APIs  

<hr>

<b>🔟 Projection vs $unset</b><br><br>

Projection:
• Controls output only  
• Does not modify database  

$unset:
• Removes field permanently from document  

<hr>

<b>Enterprise Best Practices</b><br><br>

✔ Always project only required fields  
✔ Avoid returning full documents in APIs  
✔ Combine projection with indexes  
✔ Use $project in aggregation for analytics  
✔ Exclude sensitive fields explicitly  

<hr>

<b>Expert Insight:</b><br><br>

Projection directly impacts:
• Query latency  
• Cluster network traffic  
• Memory footprint  
• Scalability of microservices  

In high-scale enterprise systems,
improper projection (returning entire documents)
can significantly degrade performance.
`
  }
]
},
{
  id: "limiting-records",
  name: "Limiting Records",
  beginner: [
  {
    content: `
<b>Limiting Records in MongoDB</b><br><br>

Limiting records means restricting the number of documents returned in the result.
MongoDB provides the limit() method for this purpose.<br><br>

<hr>

<b>1. limit()</b><br><br>

Used to specify the maximum number of documents to return.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.find().limit(number)
</pre>

<hr>

<b>2. Example</b><br><br>

<pre>
db.students.find().limit(5)
</pre>

This will return only the first 5 documents from the students collection.<br><br>

<hr>

<b>3. Using limit() with Condition</b><br><br>

<pre>
db.students.find({ city: "Pune" }).limit(3)
</pre>

This will return only 3 students from Pune.<br><br>

<hr>

<b>4. limit() with Projection</b><br><br>

<pre>
db.students.find({}, { name: 1, _id: 0 }).limit(4)
</pre>

This will show only the name field for 4 documents.<br><br>

<hr>

<b>Important Points</b><br><br>

• limit() controls number of documents returned.<br>
• Useful for pagination and performance improvement.<br>
• Works with find() method.<br><br>

<hr>

<b>Simple Example Flow</b><br><br>

Step 1: Use Database<br>
<pre>
use CollegeDB
</pre>

Step 2: Show Only 2 Records<br>
<pre>
db.students.find().limit(2)
</pre>
`
  }
],
  intermediate: [
    {
      content: `
<b>Limiting Records in MongoDB:</b><br><br>

MongoDB provides the <b>limit()</b> method to restrict the number of documents returned in a query.<br><br>

<b>Syntax:</b>
<pre>
db.collection.find().limit(number)
</pre>

<b>Example:</b>
<pre>
db.students.find().limit(5)
</pre>

- This returns only the first 5 documents from the students collection.<br>
- Useful for pagination and performance optimization.<br><br>

<b>Using limit() with sort():</b>
<pre>
db.students.find().sort({marks:-1}).limit(3)
</pre>

- Returns top 3 students based on highest marks.<br><br>

<b>Using limit() with skip() (Pagination):</b>
<pre>
db.students.find().skip(5).limit(5)
</pre>

- Skips first 5 records and returns next 5 records.<br>
- Used for page-based data display.
`
    }
  ],
  expert: [
  {
    content: `
<b>Limiting Records in MongoDB (Expert Level Guide)</b><br><br>

Limiting records in MongoDB controls how many documents
are returned from a query. This is critical for pagination,
performance optimization, API response control,
and large dataset handling.<br><br>

<hr>

<b>1️⃣ Basic limit() Syntax</b><br><br>

<pre>
db.collection.find(filter).limit(number)
</pre>

Example:

<pre>
db.customers.find().limit(5)
</pre>

Returns only the first 5 documents.<br><br>

<hr>

<b>2️⃣ limit() with sort()</b><br><br>

<pre>
db.customers.find()
  .sort({ balance: -1 })
  .limit(3)
</pre>

Returns top 3 customers with highest balance.<br><br>

Important:
Always use sort() before limit() for predictable results.<br><br>

<hr>

<b>3️⃣ Pagination Using skip() + limit()</b><br><br>

Example: Page 2 with 5 records per page

<pre>
db.customers.find()
  .skip(5)
  .limit(5)
</pre>

Formula:
skip = (pageNumber - 1) × pageSize<br><br>

Example:
Page 3, 10 records per page:
skip = (3 - 1) × 10 = 20<br><br>

<hr>

<b>4️⃣ Performance Considerations</b><br><br>

✔ limit() reduces network traffic  
✔ Improves response time  
✔ Essential for API design  
✔ Prevents memory overload  

However:

Large skip() values are inefficient because MongoDB
still scans skipped documents internally.<br><br>

For large datasets → Use range-based pagination instead.<br><br>

<hr>

<b>5️⃣ Range-Based Pagination (High Performance)</b><br><br>

Instead of skip():

<pre>
db.customers.find({ _id: { $gt: lastSeenId } })
  .limit(10)
</pre>

Advantages:
• Uses index efficiently  
• Scales better for millions of records  
• Avoids large skip overhead<br><br>

<hr>

<b>6️⃣ limit() in Aggregation Pipeline</b><br><br>

<pre>
db.transactions.aggregate([
  { $match: { amount: { $gt: 1000 } } },
  { $sort: { amount: -1 } },
  { $limit: 5 }
])
</pre>

$limit stage:
• Restricts number of documents passing to next stage  
• Reduces memory usage in pipeline<br><br>

<hr>

<b>7️⃣ limit() with Index Optimization</b><br><br>

Example:

<pre>
db.customers.createIndex({ balance: -1 })

db.customers.find()
  .sort({ balance: -1 })
  .limit(5)
  .explain("executionStats")
</pre>

If index exists:
• IXSCAN used  
• Query is very fast  

If no index:
• COLLSCAN (slow for large collections)<br><br>

<hr>

<b>8️⃣ limit() Behavior Notes</b><br><br>

• limit(0) → Returns all documents (no limit)  
• limit(n) → Returns maximum n documents  
• limit() does not guarantee order unless sort() used  

<hr>

<b>9️⃣ Real-World Use Cases</b><br><br>

• Top 10 highest transactions  
• Latest 5 login attempts  
• Display 20 products per page  
• Dashboard summary queries  

<hr>

<b>Enterprise Best Practices</b><br><br>

✔ Always combine sort() + limit()  
✔ Avoid large skip() in high-scale systems  
✔ Prefer range-based pagination  
✔ Ensure proper indexing  
✔ Use projection with limit() to reduce payload  

<hr>

<b>Expert Insight:</b><br><br>

Limit performance depends on:
• Index strategy  
• Sort field indexing  
• Shard key distribution  
• Query selectivity  

In distributed clusters,
limit() reduces data transfer between shards,
improving overall query latency.

Efficient record limiting is essential
for scalable APIs, microservices,
and real-time analytics systems.
`
  }
]
},
{
  id: "sorting-records",
  name: "Sorting Records",
  beginner: [
  {
    content: `
<b>Sorting Records in MongoDB</b><br><br>

Sorting means arranging documents in ascending or descending order.
MongoDB provides the sort() method to sort records.<br><br>

<hr>

<b>1. sort()</b><br><br>

Used to arrange documents based on a field value.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.find().sort({ field: 1 })
</pre>

• 1  → Ascending order (A-Z, 1-10)<br>
• -1 → Descending order (Z-A, 10-1)<br><br>

<hr>

<b>2. Sort in Ascending Order</b><br><br>

<pre>
db.students.find().sort({ age: 1 })
</pre>

This will sort students by age in ascending order.<br><br>

<hr>

<b>3. Sort in Descending Order</b><br><br>

<pre>
db.students.find().sort({ age: -1 })
</pre>

This will sort students by age in descending order.<br><br>

<hr>

<b>4. Sort by Multiple Fields</b><br><br>

<pre>
db.students.find().sort({ city: 1, age: -1 })
</pre>

First sorts by city (ascending), then by age (descending).<br><br>

<hr>

<b>5. sort() with limit()</b><br><br>

<pre>
db.students.find().sort({ marks: -1 }).limit(3)
</pre>

This will return top 3 students with highest marks.<br><br>

<hr>

<b>Important Points</b><br><br>

• sort() works with find() method.<br>
• 1 means ascending, -1 means descending.<br>
• Can combine with limit() and projection.<br><br>

<hr>

<b>Simple Example Flow</b><br><br>

Step 1: Use Database<br>
<pre>
use CollegeDB
</pre>

Step 2: Sort by Name<br>
<pre>
db.students.find().sort({ name: 1 })
</pre>
`
  }
],
  intermediate: [
    {
      content: `
<b>Sorting Records in MongoDB:</b><br><br>

MongoDB provides the <b>sort()</b> method to arrange documents in ascending or descending order.<br><br>

<b>Syntax:</b>
<pre>
db.collection.find().sort({ field: 1 or -1 })
</pre>

1  → Ascending order <br>
-1 → Descending order <br><br>

<b>Example (Ascending Order):</b>
<pre>
db.students.find().sort({ marks: 1 })
</pre>

- Sorts students by marks in ascending order.<br><br>

<b>Example (Descending Order):</b>
<pre>
db.students.find().sort({ marks: -1 })
</pre>

- Sorts students by marks in descending order.<br><br>

<b>Sorting Multiple Fields:</b>
<pre>
db.students.find().sort({ marks: -1, name: 1 })
</pre>

- First sorts by marks (descending).<br>
- If marks are equal, then sorts by name (ascending).<br><br>

<b>Using sort() with limit():</b>
<pre>
db.students.find().sort({ marks: -1 }).limit(3)
</pre>

- Returns top 3 students with highest marks.<br>
`
    }
  ],
  expert: [
  {
    content: `
<b>Sorting Records in MongoDB (Expert Level Guide)</b><br><br>

Sorting in MongoDB arranges query results in ascending or descending order.
Sorting is commonly combined with limit(), skip(), and indexes
to build high-performance APIs and analytics queries.<br><br>

<hr>

<b>1️⃣ Basic sort() Syntax</b><br><br>

<pre>
db.collection.find(filter).sort({ field: order })
</pre>

• 1  → Ascending order  
• -1 → Descending order  

Example:

<pre>
db.customers.find().sort({ balance: 1 })
</pre>

Sort customers by balance in ascending order.<br><br>

<hr>

<b>2️⃣ Descending Sort</b><br><br>

<pre>
db.customers.find().sort({ balance: -1 })
</pre>

Returns highest balance first.<br><br>

<hr>

<b>3️⃣ Sorting on Multiple Fields</b><br><br>

<pre>
db.customers.find()
  .sort({ accountType: 1, balance: -1 })
</pre>

First sorts by accountType (ascending),
then by balance (descending).<br><br>

Order of fields in sort object matters.<br><br>

<hr>

<b>4️⃣ Sorting with limit() (Top-N Queries)</b><br><br>

<pre>
db.customers.find()
  .sort({ balance: -1 })
  .limit(5)
</pre>

Returns top 5 customers with highest balance.<br><br>

Common use cases:
• Top 10 transactions  
• Latest 5 records  
• Highest-rated products<br><br>

<hr>

<b>5️⃣ Sorting Embedded Fields</b><br><br>

<pre>
db.users.find()
  .sort({ "address.city": 1 })
</pre>

Sorts by nested field.<br><br>

<hr>

<b>6️⃣ Sorting Arrays</b><br><br>

MongoDB sorts by:
• Minimum array value (ascending)
• Maximum array value (descending)

Example:

<pre>
db.students.find().sort({ scores: 1 })
</pre>

<hr>

<b>7️⃣ Sorting in Aggregation Pipeline</b><br><br>

<pre>
db.transactions.aggregate([
  { $match: { amount: { $gt: 1000 } } },
  { $sort: { amount: -1 } },
  { $limit: 5 }
])
</pre>

$sort stage:
• Sorts documents within pipeline  
• Often combined with $limit for performance<br><br>

<hr>

<b>8️⃣ Index & Sorting Optimization</b><br><br>

Sorting is fast when supported by an index.

Example:

<pre>
db.customers.createIndex({ balance: -1 })

db.customers.find()
  .sort({ balance: -1 })
  .explain("executionStats")
</pre>

If optimized:
• IXSCAN used  
• No in-memory sort  

If not indexed:
• COLLSCAN  
• In-memory sort  
• Slower for large datasets<br><br>

<hr>

<b>9️⃣ In-Memory Sort & Limits</b><br><br>

MongoDB has memory limits for sorting.
If sort exceeds memory:
• It may fail unless allowDiskUse is enabled (aggregation)

Example:

<pre>
db.transactions.aggregate(
  [
    { $sort: { amount: -1 } }
  ],
  { allowDiskUse: true }
)
</pre>

<hr>

<b>🔟 Collation-Based Sorting</b><br><br>

Case-insensitive sorting:

<pre>
db.users.find()
  .collation({ locale: "en", strength: 2 })
  .sort({ name: 1 })
</pre>

Useful for:
• Alphabetical sorting  
• Multilingual systems<br><br>

<hr>

<b>1️⃣1️⃣ Sorting in Sharded Clusters</b><br><br>

• If sorting on shard key → Efficient  
• If not → Scatter-gather + merge sort  
• May increase network traffic  

Proper shard key design improves sorting performance.<br><br>

<hr>

<b>Performance Best Practices</b><br><br>

✔ Always index frequently sorted fields  
✔ Combine sort() + limit() for top-N queries  
✔ Avoid sorting large datasets without index  
✔ Use compound indexes for multi-field sort  
✔ Ensure index order matches sort order  

Example compound index:

<pre>
db.customers.createIndex({ accountType: 1, balance: -1 })
</pre>

<hr>

<b>Expert Insight:</b><br><br>

Sorting performance depends on:
• Index design  
• Data volume  
• Memory limits  
• Shard key selection  
• Query selectivity  

Unindexed sorting in large collections
can cause heavy memory usage,
disk spillover, and slow response times.

Efficient sorting is critical in dashboards,
banking reports, analytics systems,
and high-scale microservices.
`
  }
]
},
{
  id: "indexing",
  name: "Indexing in MongoDB",
  beginner: [
  {
    content: `
<b>Indexing in MongoDB</b><br><br>

Indexing is used to improve the performance of search queries.
An index stores a small portion of data in an easy-to-search format.<br><br>

Without index → MongoDB scans all documents (slow).<br>
With index → MongoDB finds data quickly (fast).<br><br>

<hr>

<b>1. Create Index</b><br><br>

Used to create an index on a field.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.createIndex({ field: 1 })
</pre>

• 1  → Ascending index<br>
• -1 → Descending index<br><br>

<b>Example:</b><br>
<pre>
db.students.createIndex({ name: 1 })
</pre>

This creates an index on the name field.<br><br>

<hr>

<b>2. Create Descending Index</b><br><br>

<pre>
db.students.createIndex({ age: -1 })
</pre>

<hr>

<b>3. View Indexes</b><br><br>

<pre>
db.students.getIndexes()
</pre>

<hr>

<b>4. Drop Index</b><br><br>

<pre>
db.students.dropIndex({ name: 1 })
</pre>

<hr>

<b>Important Points</b><br><br>

• Index improves query performance.<br>
• Too many indexes slow down insert and update operations.<br>
• MongoDB automatically creates index on _id field.<br>
• Use index on frequently searched fields.<br><br>

<hr>

<b>Simple Example Flow</b><br><br>

Step 1: Use Database<br>
<pre>
use CollegeDB
</pre>

Step 2: Create Index<br>
<pre>
db.students.createIndex({ email: 1 })
</pre>

Step 3: Check Index<br>
<pre>
db.students.getIndexes()
</pre>
`
  }
],
  intermediate: [
    {
      content: `
<b>Indexing in MongoDB:</b><br><br>

Indexing improves the speed of search queries in MongoDB.<br>
Without index, MongoDB performs full collection scan.<br><br>

<b>What is Index?</b><br>
- An index is a special data structure that stores small portion of collection data.<br>
- It improves performance of read operations.<br><br>

<b>Create Index:</b>
<pre>
db.collection.createIndex({ field: 1 })
</pre>

<b>Example:</b>
<pre>
db.students.createIndex({ name: 1 })
</pre>

- Creates ascending index on name field.<br><br>

<b>Descending Index:</b>
<pre>
db.students.createIndex({ marks: -1 })
</pre>

1  → Ascending index <br>
-1 → Descending index <br><br>

<b>Compound Index:</b>
<pre>
db.students.createIndex({ marks: -1, name: 1 })
</pre>

- Index on multiple fields.<br><br>

<b>View Indexes:</b>
<pre>
db.students.getIndexes()
</pre>

<b>Drop Index:</b>
<pre>
db.students.dropIndex({ name: 1 })
</pre>

<b>Advantages:</b><br>
- Faster query performance<br>
- Efficient sorting<br>
- Improves read operations<br><br>

<b>Disadvantages:</b><br>
- Uses extra storage<br>
- Slows down insert/update operations
`
    }
  ],
  expert: [
  {
    content: `
<b>Indexing in MongoDB (Expert Level Guide)</b><br><br>

Indexing in MongoDB improves query performance by creating
a data structure (B-Tree based in WiredTiger) that allows
fast lookups without scanning the entire collection.

Without indexes → COLLSCAN (Full Collection Scan)  
With indexes → IXSCAN (Index Scan – Fast)<br><br>

<hr>

<b>1️⃣ Create Index</b><br><br>

<pre>
db.collection.createIndex({ field: 1 })
</pre>

• 1  → Ascending  
• -1 → Descending  

Example:

<pre>
db.customers.createIndex({ email: 1 })
</pre>

<hr>

<b>2️⃣ Unique Index</b><br><br>

Prevents duplicate values.

<pre>
db.users.createIndex(
  { email: 1 },
  { unique: true }
)
</pre>

Used in:
• Authentication systems  
• Banking account numbers  
• Business keys<br><br>

<hr>

<b>3️⃣ Compound Index</b><br><br>

Index on multiple fields.

<pre>
db.customers.createIndex(
  { accountType: 1, balance: -1 }
)
</pre>

Important Rule:
Query must follow prefix order.

✔ Works:
{ accountType: "Savings" }  
{ accountType: "Savings", balance: { $gt: 5000 } }

❌ Does NOT efficiently work:
{ balance: { $gt: 5000 } } only<br><br>

<hr>

<b>4️⃣ Multikey Index (Array Fields)</b><br><br>

Automatically created when indexing array fields.

<pre>
db.students.createIndex({ skills: 1 })
</pre>

Each array element becomes indexed.<br><br>

<hr>

<b>5️⃣ Text Index</b><br><br>

Used for text search.

<pre>
db.products.createIndex({ description: "text" })

db.products.find({
  $text: { $search: "laptop" }
})
</pre>

Only one text index per collection.<br><br>

<hr>

<b>6️⃣ TTL Index (Auto Expiry)</b><br><br>

Automatically deletes documents after specified time.

<pre>
db.sessions.createIndex(
  { createdAt: 1 },
  { expireAfterSeconds: 3600 }
)
</pre>

Used in:
• OTP systems  
• Session management  
• Logs cleanup<br><br>

<hr>

<b>7️⃣ Sparse Index</b><br><br>

Indexes only documents that contain the field.

<pre>
db.users.createIndex(
  { middleName: 1 },
  { sparse: true }
)
</pre>

<hr>

<b>8️⃣ Partial Index</b><br><br>

Indexes only documents matching a condition.

<pre>
db.customers.createIndex(
  { balance: 1 },
  { partialFilterExpression: { balance: { $gt: 0 } } }
)
</pre>

Improves performance and reduces index size.<br><br>

<hr>

<b>9️⃣ Hashed Index</b><br><br>

Used mainly for sharding.

<pre>
db.users.createIndex({ userId: "hashed" })
</pre>

Distributes data evenly across shards.<br><br>

<hr>

<b>🔟 Geospatial Index</b><br><br>

<pre>
db.places.createIndex({ location: "2dsphere" })
</pre>

Used for:
• Location-based search  
• Ride-sharing apps  
• Delivery tracking<br><br>

<hr>

<b>1️⃣1️⃣ Covered Query (Maximum Optimization)</b><br><br>

If query + projection fields are fully covered by index:

• No document fetch  
• Extremely fast  

Example:

<pre>
db.customers.createIndex({ email: 1, balance: 1 })

db.customers.find(
  { email: "amit@gmail.com" },
  { email: 1, balance: 1, _id: 0 }
).explain("executionStats")
</pre>

Should show:
• IXSCAN  
• No FETCH stage<br><br>

<hr>

<b>1️⃣2️⃣ Analyze Index Usage</b><br><br>

<pre>
db.collection.getIndexes()
db.collection.find(query).explain("executionStats")
</pre>

Look for:
• IXSCAN vs COLLSCAN  
• executionTimeMillis  
• totalDocsExamined  
• totalKeysExamined<br><br>

<hr>

<b>1️⃣3️⃣ Index Trade-Offs</b><br><br>

Advantages:
✔ Faster reads  
✔ Efficient sorting  
✔ Supports covered queries  

Disadvantages:
✖ Slower writes (insert/update/delete)  
✖ More storage usage  
✖ Increased memory usage  

More indexes = Slower write performance.<br><br>

<hr>

<b>1️⃣4️⃣ Indexing in Sharded Clusters</b><br><br>

• Shard key must be indexed  
• Proper index design reduces scatter-gather queries  
• Compound shard key improves query targeting<br><br>

<hr>

<b>Enterprise Best Practices</b><br><br>

✔ Index frequently queried fields  
✔ Index fields used in sort()  
✔ Keep index count minimal  
✔ Use compound indexes wisely  
✔ Avoid over-indexing  
✔ Monitor with profiler & explain()  
✔ Regularly review unused indexes  

<hr>

<b>Expert Insight:</b><br><br>

Index performance depends on:
• Cardinality of field  
• Query selectivity  
• Data distribution  
• Working set size (RAM)  
• Shard key strategy  

Poor indexing leads to:
• Full collection scans  
• High CPU usage  
• Memory pressure  
• Replication lag  

Well-designed indexing strategy is critical
for high-scale banking, e-commerce,
analytics, and real-time systems.
`
  }
]
},
{
  id: "aggregation",
  name: "Aggregation in MongoDB",
  beginner: [
  {
    content: `
<b>Aggregation in MongoDB</b><br><br>

Aggregation is used to process data and return calculated results.
It is similar to GROUP BY in SQL.<br><br>

Aggregation is performed using the aggregate() method.<br><br>

<hr>

<b>1. Basic Syntax</b><br><br>

<pre>
db.collectionName.aggregate([
   { stage1 },
   { stage2 }
])
</pre>

Aggregation works in stages (pipeline).<br><br>

<hr>

<b>2. $match (Filter Data)</b><br><br>

Used to filter documents.<br><br>

<pre>
db.students.aggregate([
   { $match: { city: "Pune" } }
])
</pre>

<hr>

<b>3. $group (Group Data)</b><br><br>

Used to group documents and perform calculations.<br><br>

<pre>
db.students.aggregate([
   {
     $group: {
        _id: "$city",
        totalStudents: { $sum: 1 }
     }
   }
])
</pre>

This groups students by city and counts them.<br><br>

<hr>

<b>4. $sort (Sort Result)</b><br><br>

<pre>
db.students.aggregate([
   { $sort: { marks: -1 } }
])
</pre>

<hr>

<b>5. $limit (Limit Result)</b><br><br>

<pre>
db.students.aggregate([
   { $limit: 3 }
])
</pre>

<hr>

<b>Important Points</b><br><br>

• aggregate() uses pipeline stages.<br>
• $match filters data.<br>
• $group performs calculations.<br>
• $sort sorts the result.<br>
• $limit limits the output.<br><br>

<hr>

<b>Simple Example Flow</b><br><br>

Step 1: Use Database<br>
<pre>
use CollegeDB
</pre>

Step 2: Count Students by City<br>
<pre>
db.students.aggregate([
   {
     $group: {
        _id: "$city",
        total: { $sum: 1 }
     }
   }
])
</pre>
`
  }
],
  intermediate: [
    {
      content: `
<b>Aggregation in MongoDB:</b><br><br>

Aggregation is used to process data records and return computed results.<br>
It performs operations like grouping, filtering, sorting, and calculating totals.<br><br>

MongoDB uses <b>aggregate()</b> method with pipeline stages.<br><br>

<b>Syntax:</b>
<pre>
db.collection.aggregate([
  { stage1 },
  { stage2 }
])
</pre>

Each stage transforms the documents.<br><br>

<b>Common Aggregation Stages:</b><br>

<b>1. $match (Filtering)</b>
<pre>
db.students.aggregate([
  { $match: { marks: { $gt: 60 } } }
])
</pre>
- Filters students with marks greater than 60.<br><br>

<b>2. $group (Grouping)</b>
<pre>
db.students.aggregate([
  { $group: { _id: "$department", total: { $sum: "$marks" } } }
])
</pre>
- Groups students by department and calculates total marks.<br><br>

<b>3. $sort</b>
<pre>
db.students.aggregate([
  { $sort: { marks: -1 } }
])
</pre>

<b>4. $limit</b>
<pre>
db.students.aggregate([
  { $limit: 5 }
])
</pre>

<b>5. $project</b>
<pre>
db.students.aggregate([
  { $project: { name: 1, marks: 1, _id: 0 } }
])
</pre>
- Displays selected fields only.<br><br>

<b>Example (Complete Pipeline):</b>
<pre>
db.students.aggregate([
  { $match: { marks: { $gt: 50 } } },
  { $group: { _id: "$department", avgMarks: { $avg: "$marks" } } },
  { $sort: { avgMarks: -1 } }
])
</pre>

- Filters students with marks > 50<br>
- Groups by department<br>
- Calculates average marks<br>
- Sorts by average marks descending<br>
`
    }
  ],
  expert: [
  {
    content: `
<b>Aggregation in MongoDB (Expert Level Guide)</b><br><br>

Aggregation in MongoDB is a framework used for data processing,
transformation, analytics, and reporting. It allows you to
process documents through multiple pipeline stages,
similar to SQL GROUP BY, HAVING, JOIN, and complex analytics queries.<br><br>

Aggregation uses a pipeline model:
Documents pass through stages → Each stage transforms output → Final result returned.<br><br>

<hr>

<b>1️⃣ Basic Aggregation Syntax</b><br><br>

<pre>
db.collection.aggregate([
  { stage1 },
  { stage2 },
  ...
])
</pre>

Example:

<pre>
db.transactions.aggregate([
  { $match: { amount: { $gt: 1000 } } },
  { $group: { _id: "$accountType", totalAmount: { $sum: "$amount" } } }
])
</pre>

<hr>

<b>2️⃣ Important Aggregation Stages</b><br><br>

<b>$match</b> → Filter documents (like WHERE)

<pre>
{ $match: { status: "Success" } }
</pre>

<b>$group</b> → Group documents (like GROUP BY)

<pre>
{
  $group: {
    _id: "$accountType",
    total: { $sum: "$amount" },
    avgAmount: { $avg: "$amount" },
    count: { $sum: 1 }
  }
}
</pre>

<b>$sort</b> → Sort results

<pre>
{ $sort: { total: -1 } }
</pre>

<b>$limit</b> → Limit records

<pre>
{ $limit: 5 }
</pre>

<b>$project</b> → Select/transform fields

<pre>
{
  $project: {
    accountType: "$_id",
    total: 1,
    _id: 0
  }
}
</pre>

<hr>

<b>3️⃣ Full Example (Banking Report)</b><br><br>

<pre>
db.transactions.aggregate([
  { $match: { amount: { $gt: 1000 } } },
  {
    $group: {
      _id: "$accountType",
      totalAmount: { $sum: "$amount" },
      transactionCount: { $sum: 1 }
    }
  },
  { $sort: { totalAmount: -1 } },
  { $limit: 3 }
])
</pre>

Generates:
Top 3 account types by total transaction amount.<br><br>

<hr>

<b>4️⃣ $lookup (Join Operation)</b><br><br>

Equivalent to SQL JOIN.

<pre>
db.orders.aggregate([
  {
    $lookup: {
      from: "customers",
      localField: "customerId",
      foreignField: "_id",
      as: "customerDetails"
    }
  }
])
</pre>

Used in:
• E-commerce  
• Banking customer transactions  
• Microservices integration<br><br>

<hr>

<b>5️⃣ $unwind (Array Deconstruction)</b><br><br>

<pre>
{ $unwind: "$items" }
</pre>

Breaks array into multiple documents.<br><br>

<hr>

<b>6️⃣ $addFields / $set</b><br><br>

Add computed fields:

<pre>
{
  $addFields: {
    tax: { $multiply: ["$amount", 0.18] }
  }
}
</pre>

<hr>

<b>7️⃣ $facet (Multiple Pipelines)</b><br><br>

<pre>
db.products.aggregate([
  {
    $facet: {
      expensive: [
        { $match: { price: { $gt: 1000 } } }
      ],
      cheap: [
        { $match: { price: { $lte: 1000 } } }
      ]
    }
  }
])
</pre>

Used in dashboards & analytics.<br><br>

<hr>

<b>8️⃣ $bucket (Range Grouping)</b><br><br>

<pre>
{
  $bucket: {
    groupBy: "$amount",
    boundaries: [0, 1000, 5000, 10000],
    default: "Other",
    output: { count: { $sum: 1 } }
  }
}
</pre>

Used in:
• Financial categorization  
• Analytics histograms<br><br>

<hr>

<b>9️⃣ Performance Optimization</b><br><br>

✔ Place $match early in pipeline  
✔ Use indexes for $match stage  
✔ Use $project to reduce fields  
✔ Use $limit after $sort for top-N queries  
✔ Enable allowDiskUse for large datasets  

Example:

<pre>
db.transactions.aggregate(
  [ { $sort: { amount: -1 } } ],
  { allowDiskUse: true }
)
</pre>

<hr>

<b>🔟 Aggregation vs find()</b><br><br>

find():
• Simple filtering  
• Faster for basic queries  

aggregate():
• Complex analytics  
• Transformations  
• Joins  
• Grouping  
• Reporting  

<hr>

<b>1️⃣1️⃣ Aggregation in Sharded Cluster</b><br><br>

• $match on shard key improves routing  
• Pipeline executed partly on shards  
• Merge phase on mongos  
• Improper design → heavy network usage  

<hr>

<b>Enterprise Best Practices</b><br><br>

✔ Design pipeline carefully  
✔ Keep pipeline stages minimal  
✔ Use indexes  
✔ Avoid unnecessary $unwind  
✔ Monitor with explain("executionStats")  
✔ Optimize memory usage  

<hr>

<b>Expert Insight:</b><br><br>

Aggregation performance depends on:
• Index support  
• Pipeline stage order  
• Data distribution  
• Document size  
• Shard key selection  

Improper aggregation can cause:
• Memory overflow  
• Disk spill  
• Slow dashboard loading  
• High cluster resource usage  

Well-optimized aggregation is critical for:
• Banking reports  
• E-commerce analytics  
• Real-time dashboards  
• Fraud detection systems  
• Enterprise BI pipelines  
`
  }
]
},
{
  id: "replication",
  name: "Replication in MongoDB",
  beginner: [
  {
    content: `
<b>Replication in MongoDB</b><br><br>

Replication means copying data from one server to other servers.
It helps in data backup and high availability.<br><br>

MongoDB uses Replica Sets for replication.<br><br>

<hr>

<b>1. What is a Replica Set?</b><br><br>

A Replica Set is a group of MongoDB servers that maintain the same data.<br><br>

It consists of:<br>
• Primary Node<br>
• Secondary Node(s)<br><br>

<hr>

<b>2. Primary Node</b><br><br>

• Handles all write operations (insert, update, delete).<br>
• Sends data to secondary nodes.<br><br>

<hr>

<b>3. Secondary Node</b><br><br>

• Copies data from primary node.<br>
• Used for backup and read operations (optional).<br><br>

<hr>

<b>4. How Replication Works</b><br><br>

1. Client writes data to Primary.<br>
2. Primary records the operation in oplog (operation log).<br>
3. Secondary nodes copy data from oplog.<br><br>

<hr>

<b>5. Advantages of Replication</b><br><br>

• Data backup<br>
• High availability<br>
• Automatic failover<br>
• Improved read performance<br><br>

<hr>

<b>6. Basic Command to Initialize Replica Set</b><br><br>

<pre>
rs.initiate()
</pre>

Check replica set status:<br>

<pre>
rs.status()
</pre>

<hr>

<b>Important Points</b><br><br>

• Replication prevents data loss.<br>
• If primary fails, one secondary becomes new primary automatically.<br>
• Used in production environments.<br><br>

<hr>

<b>Simple Example Concept</b><br><br>

Server1 → Primary<br>
Server2 → Secondary<br>
Server3 → Secondary<br><br>

All servers contain same data.
`
  }
],
  intermediate: [
    {
      content: `
<b>Replication in MongoDB:</b><br><br>

Replication is the process of synchronizing data across multiple servers.<br>
It provides high availability and data redundancy.<br><br>

MongoDB uses <b>Replica Set</b> for replication.<br><br>

<b>What is Replica Set?</b><br>
- A group of MongoDB servers that maintain the same data set.<br>
- Ensures automatic failover and data recovery.<br><br>

<b>Components of Replica Set:</b><br>

<b>1. Primary Node:</b><br>
- Accepts all write operations.<br>
- Only one primary in a replica set.<br><br>

<b>2. Secondary Node:</b><br>
- Copies data from primary.<br>
- Handles read operations (optional).<br><br>

<b>3. Arbiter (Optional):</b><br>
- Participates in elections.<br>
- Does not store data.<br><br>

<b>Working of Replication:</b><br>
- Primary node receives write request.<br>
- Changes are recorded in oplog (operation log).<br>
- Secondary nodes replicate data from oplog.<br>
- If primary fails, automatic election selects new primary.<br><br>

<b>Advantages:</b><br>
- High availability<br>
- Data backup<br>
- Automatic failover<br>
- Disaster recovery<br><br>

<b>Example Command to Initiate Replica Set:</b>
<pre>
rs.initiate()
</pre>

<b>Check Replica Status:</b>
<pre>
rs.status()
</pre>
`
    }
  ]
},
{
  id: "sharding",
  name: "Sharding in MongoDB",
  beginner: [
  {
    content: `
<b>Sharding in MongoDB</b><br><br>

Sharding means distributing data across multiple servers.
It is used when the database becomes very large.<br><br>

Sharding helps in horizontal scaling.<br><br>

<hr>

<b>1. Why Sharding is Needed?</b><br><br>

• When data size is very large.<br>
• When a single server cannot handle traffic.<br>
• To improve performance.<br><br>

<hr>

<b>2. What is a Shard?</b><br><br>

A Shard is a MongoDB server that stores part of the data.<br><br>

Each shard holds a portion of the total data.<br><br>

<hr>

<b>3. Main Components of Sharding</b><br><br>

• Shard → Stores data<br>
• Config Server → Stores metadata<br>
• Mongos → Query Router<br><br>

<hr>

<b>4. How Sharding Works</b><br><br>

1. Data is divided using a Shard Key.<br>
2. Data is distributed across multiple shards.<br>
3. Mongos routes queries to correct shard.<br><br>

<hr>

<b>5. Example Concept</b><br><br>

Suppose we have 1 million student records:<br><br>

Shard1 → Students 1 - 300000<br>
Shard2 → Students 300001 - 600000<br>
Shard3 → Students 600001 - 1000000<br><br>

Data is divided across servers.<br><br>

<hr>

<b>Important Points</b><br><br>

• Sharding is used for large-scale applications.<br>
• It provides horizontal scaling.<br>
• Requires a shard key.<br>
• Used in big production systems.<br><br>

<hr>

<b>Sharding vs Replication</b><br><br>

Replication → Copies same data to multiple servers (backup).<br>
Sharding → Splits data across multiple servers (scaling).<br><br>
`
  }
],
  intermediate: [
    {
      content: `
<b>Sharding in MongoDB:</b><br><br>

Sharding is the process of distributing data across multiple machines.<br>
It is used to handle large datasets and high traffic.<br><br>

Sharding provides <b>horizontal scaling</b>.<br><br>

<b>Why Sharding is Needed?</b><br>
- When data size increases beyond single server capacity.<br>
- When high read/write operations are required.<br>
- To improve performance and scalability.<br><br>

<b>Sharded Cluster Components:</b><br>

<b>1. Shards:</b><br>
- Store actual data.<br>
- Each shard holds a portion of total data.<br><br>

<b>2. Config Server:</b><br>
- Stores metadata and cluster configuration.<br><br>

<b>3. Query Router (mongos):</b><br>
- Routes client requests to appropriate shard.<br><br>

<b>Shard Key:</b><br>
- A field used to distribute data across shards.<br>
- Important for balanced data distribution.<br><br>

<b>Example (Enable Sharding):</b>
<pre>
sh.enableSharding("schoolDB")
</pre>

<b>Shard a Collection:</b>
<pre>
sh.shardCollection("schoolDB.students", { rollNo: 1 })
</pre>

<b>Advantages:</b><br>
- Horizontal scalability<br>
- Handles big data efficiently<br>
- Improves performance<br><br>

<b>Difference Between Replication and Sharding:</b><br>
Replication → Copies same data for high availability<br>
Sharding → Distributes data for scalability<br>
`
    }
  ]
},
{
  id: "mongodb-backup",
  name: "Create Backup in MongoDB",
  beginner: [
  {
    content: `
<b>Create Backup in MongoDB</b><br><br>

Backup means creating a copy of your database data.
It helps to restore data in case of system failure or data loss.<br><br>

MongoDB provides mongodump and mongorestore tools for backup and restore.<br><br>

<hr>

<b>1. mongodump (Create Backup)</b><br><br>

Used to take backup of database.<br><br>

<b>Syntax:</b><br>
<pre>
mongodump --db databaseName --out backupFolderPath
</pre>

<b>Example:</b><br>
<pre>
mongodump --db CollegeDB --out C:\\backup
</pre>

This creates a backup of CollegeDB in the backup folder.<br><br>

<hr>

<b>2. Backup All Databases</b><br><br>

<pre>
mongodump --out C:\\backup
</pre>

This will backup all databases.<br><br>

<hr>

<b>3. mongorestore (Restore Backup)</b><br><br>

Used to restore data from backup.<br><br>

<b>Syntax:</b><br>
<pre>
mongorestore --db databaseName backupFolderPath
</pre>

<b>Example:</b><br>
<pre>
mongorestore --db CollegeDB C:\\backup\\CollegeDB
</pre>

<hr>

<b>4. Backup Single Collection</b><br><br>

<pre>
mongodump --db CollegeDB --collection students --out C:\\backup
</pre>

<hr>

<b>Important Points</b><br><br>

• Backup prevents data loss.<br>
• Use regular backups in production.<br>
• mongodump creates BSON files.<br>
• mongorestore restores the data.<br><br>

<hr>

<b>Simple Example Flow</b><br><br>

Step 1: Open Command Prompt<br>
Step 2: Run mongodump command<br>
Step 3: Check backup folder<br>
Step 4: Use mongorestore if needed<br>
`
  }
],
  intermediate: [
    {
      content: `
<b>Backup in MongoDB:</b><br><br>

Backup is the process of creating a copy of database data.<br>
It helps in data recovery in case of system failure or data loss.<br><br>

MongoDB provides command-line tools for backup and restore.<br><br>

<b>1. mongodump (Create Backup)</b><br>
Used to create a backup of database.<br><br>

<b>Syntax:</b>
<pre>
mongodump --db databaseName --out backupFolderPath
</pre>

<b>Example:</b>
<pre>
mongodump --db schoolDB --out C:\\backup
</pre>

- Creates backup of schoolDB database.<br>
- Stores backup files in specified folder.<br><br>

<b>Backup Entire Server:</b>
<pre>
mongodump --out C:\\backup
</pre>

- Backs up all databases.<br><br>

<b>2. mongorestore (Restore Backup)</b><br>

<b>Syntax:</b>
<pre>
mongorestore --db databaseName backupFolderPath
</pre>

<b>Example:</b>
<pre>
mongorestore --db schoolDB C:\\backup\\schoolDB
</pre>

- Restores the backed-up database.<br><br>

<b>Advantages of Backup:</b><br>
- Data recovery<br>
- Disaster protection<br>
- Safe migration<br>
- Business continuity<br>
`
    }
  ]
},
{
  id: "relationships",
  name: "Relationships in MongoDB",
  beginner: [
  {
    content: `
<b>Relationships in MongoDB</b><br><br>

Relationships define how data in one collection is connected to data in another collection.
MongoDB supports relationships using two main approaches:<br><br>

1. Embedded Documents<br>
2. Referenced Documents<br><br>

<hr>

<b>1. Embedded Documents (Embedding)</b><br><br>

In this method, related data is stored inside the same document.<br><br>

<b>Example:</b><br>
<pre>
db.students.insertOne({
  name: "Pratiksha",
  age: 22,
  address: {
    city: "Pune",
    state: "Maharashtra"
  }
})
</pre>

Here, address is embedded inside student document.<br><br>

<b>Advantages:</b><br>
• Faster read operations<br>
• Simple structure<br><br>

<hr>

<b>2. Referenced Documents (Referencing)</b><br><br>

In this method, related data is stored in separate collections using reference (ObjectId).<br><br>

<b>Example:</b><br>

Students Collection:<br>
<pre>
{
  _id: ObjectId("101"),
  name: "Pratiksha",
  courseId: ObjectId("201")
}
</pre>

Courses Collection:<br>
<pre>
{
  _id: ObjectId("201"),
  courseName: "Java"
}
</pre>

Here, courseId links student to course.<br><br>

<hr>

<b>When to Use Embedding?</b><br><br>

• One-to-One relationship<br>
• Small related data<br>
• Data frequently accessed together<br><br>

<hr>

<b>When to Use Referencing?</b><br><br>

• One-to-Many relationship<br>
• Large related data<br>
• Data updated frequently<br><br>

<hr>

<b>Important Points</b><br><br>

• MongoDB does not support JOIN like SQL directly.<br>
• Relationships are handled using embedding or referencing.<br>
• Choose method based on application needs.<br><br>

<hr>

<b>Simple Example Concept</b><br><br>

Embedding → Student contains address.<br>
Referencing → Student references Course using courseId.<br>
`
  }
],
  intermediate: [
    {
      content: `
<b>Relationships in MongoDB:</b><br><br>

MongoDB is a NoSQL database and does not support JOIN like SQL.<br>
Relationships are managed using:<br>
1. Embedding<br>
2. Referencing<br><br>

============================<br>
<b>1. Embedded Documents</b><br>
============================<br>

- Related data is stored inside a single document.<br>
- Improves read performance.<br>
- Suitable for One-to-One and One-to-Few relationships.<br><br>

<b>Example:</b>
<pre>
{
  name: "Pratiksha",
  age: 22,
  address: {
    city: "Pune",
    state: "Maharashtra"
  }
}
</pre>

Advantages:<br>
- Fast read operation<br>
- Single query required<br><br>

Disadvantages:<br>
- Data duplication possible<br>
- Large document size<br><br>

============================<br>
<b>2. Referencing</b><br>
============================<br>

- Store reference (ObjectId) of another document.<br>
- Similar to foreign key in SQL.<br>
- Suitable for large and frequently changing data.<br><br>

<b>Example:</b>

Students Collection:
<pre>
{
  _id: ObjectId("123"),
  name: "Pratiksha"
}
</pre>

Courses Collection:
<pre>
{
  student_id: ObjectId("123"),
  course: "MongoDB"
}
</pre>

- Reference is created using ObjectId.<br><br>

============================<br>
<b>Types of Relationships</b><br>
============================<br>

1. One-to-One<br>
2. One-to-Many<br>
3. Many-to-Many<br><br>

MongoDB handles relationships using embedding or referencing based on use case.<br>
`
    }
  ]
},
{
  id: "covered-queries",
  name: "Covered Queries in MongoDB",
  beginner: [
  {
    content: `
<b>Covered Queries in MongoDB</b><br><br>

A Covered Query is a query that can be answered using only an index,
without scanning the actual documents.<br><br>

This improves performance because MongoDB does not need to read full documents.<br><br>

<hr>

<b>1. What is Required for Covered Query?</b><br><br>

• All fields in the query condition must be in the index.<br>
• All fields in the projection must be in the same index.<br>
• _id must be excluded (if not part of index).<br><br>

<hr>

<b>2. Example</b><br><br>

Step 1: Create Index<br>

<pre>
db.students.createIndex({ name: 1, age: 1 })
</pre>

Step 2: Run Covered Query<br>

<pre>
db.students.find(
   { name: "Pratiksha" },
   { name: 1, age: 1, _id: 0 }
)
</pre>

In this case:<br>
• Query field (name) is indexed.<br>
• Projection fields (name, age) are indexed.<br>
• _id is excluded.<br><br>

So this becomes a Covered Query.<br><br>

<hr>

<b>3. How to Check Covered Query?</b><br><br>

Use explain() method:<br>

<pre>
db.students.find(
   { name: "Pratiksha" },
   { name: 1, age: 1, _id: 0 }
).explain("executionStats")
</pre>

If it shows "IXSCAN" and no "COLLSCAN", it means index is used.<br><br>

<hr>

<b>Important Points</b><br><br>

• Covered Query improves performance.<br>
• Reduces disk I/O.<br>
• Requires proper indexing.<br>
• Works best with projection.<br><br>

<hr>

<b>Simple Concept</b><br><br>

Normal Query → Reads full documents.<br>
Covered Query → Reads only index.<br><br>
`
  }
],
  intermediate: [
    {
      content: `
<b>Covered Queries in MongoDB:</b><br><br>

A Covered Query is a query that can be satisfied entirely using an index,
without scanning the actual documents.<br><br>

If all the fields used in:
- Query condition
- Projection

are part of the index, then MongoDB does not need to read the document.<br><br>

This improves performance significantly.<br><br>

============================<br>
<b>Conditions for Covered Query:</b><br>
============================<br>

1. Query must use an index.<br>
2. All queried fields must be part of the index.<br>
3. Projection should exclude _id (if not indexed).<br><br>

============================<br>
<b>Example:</b><br>
============================<br>

Create Index:
<pre>
db.students.createIndex({ name: 1, marks: 1 })
</pre>

Covered Query:
<pre>
db.students.find(
  { name: "Pratiksha" },
  { name: 1, marks: 1, _id: 0 }
)
</pre>

- Query uses indexed fields only.<br>
- No document scan required.<br><br>

============================<br>
<b>Non-Covered Query Example:</b><br>
============================<br>

<pre>
db.students.find(
  { name: "Pratiksha" },
  { name: 1, age: 1 }
)
</pre>

- If age is not indexed, MongoDB reads full document.<br>
- Not a covered query.<br><br>

============================<br>
<b>Advantages:</b><br>
============================<br>

- Faster performance<br>
- Reduced disk I/O<br>
- Efficient for large datasets<br>
`
    }
  ]
},
{
  id: "cassandra-vs-mongodb",
  name: "Cassandra vs MongoDB",
  beginner: [
  {
    content: `
<b>Cassandra vs MongoDB</b><br><br>

Both Cassandra and MongoDB are NoSQL databases.
They are used to handle large amounts of data,
but their design and use cases are different.<br><br>

<hr>

<b>1. Database Type</b><br><br>

MongoDB → Document-based database (stores data in JSON-like format).<br>
Cassandra → Wide-column store database.<br><br>

<hr>

<b>2. Data Model</b><br><br>

MongoDB → Stores data in documents (BSON format).<br>
Cassandra → Stores data in rows and columns (table format).<br><br>

<hr>

<b>3. Query Language</b><br><br>

MongoDB → Uses MongoDB Query Language (MQL).<br>
Cassandra → Uses CQL (Cassandra Query Language).<br><br>

<hr>

<b>4. Scalability</b><br><br>

MongoDB → Supports horizontal scaling using Sharding.<br>
Cassandra → Designed for high horizontal scalability by default.<br><br>

<hr>

<b>5. Replication</b><br><br>

MongoDB → Uses Replica Sets.<br>
Cassandra → Built-in replication across multiple nodes.<br><br>

<hr>

<b>6. Best Use Cases</b><br><br>

MongoDB → Applications needing flexible schema and fast development.<br>
Cassandra → Applications requiring high availability and massive data handling.<br><br>

<hr>

<b>Comparison Table</b><br><br>

Feature            | MongoDB              | Cassandra
---------------------------------------------------------
Database Type      | Document-based       | Wide-column
Schema             | Flexible             | Fixed (table-based)
Scalability        | Sharding             | Built-in distributed
Best For           | Web apps, APIs       | Large-scale systems
Query Language     | MQL                  | CQL
<br><br>

<hr>

<b>Important Points</b><br><br>

• MongoDB is easier for beginners.<br>
• Cassandra is preferred for very large distributed systems.<br>
• Both are NoSQL databases.<br><br>

<hr>

<b>Simple Concept</b><br><br>

MongoDB → Flexible and developer-friendly.<br>
Cassandra → Highly scalable and fault-tolerant.<br><br>
`
  }
],
  intermediate: [
    {
      content: `
<b>Cassandra vs MongoDB:</b><br><br>

Both Cassandra and MongoDB are NoSQL databases,
but they are designed for different purposes.<br><br>

============================<br>
<b>1. Data Model</b><br>
============================<br>

MongoDB:
- Document-oriented database.
- Stores data in BSON format.
- Flexible schema (dynamic).

Cassandra:
- Wide-column store database.
- Uses tables with rows and columns.
- Schema required before inserting data.<br><br>

============================<br>
<b>2. Architecture</b><br>
============================<br>

MongoDB:
- Uses Replica Sets for replication.
- Uses Sharding for scaling.
- Primary-Secondary architecture.

Cassandra:
- Fully distributed architecture.
- No master node (Peer-to-Peer).
- All nodes are equal.<br><br>

============================<br>
<b>3. Scalability</b><br>
============================<br>

MongoDB:
- Supports horizontal scaling using sharding.

Cassandra:
- Designed for massive horizontal scaling.
- Handles very large datasets easily.<br><br>

============================<br>
<b>4. Performance</b><br>
============================<br>

MongoDB:
- Good for read-heavy applications.
- Strong indexing support.

Cassandra:
- Excellent for write-heavy workloads.
- High availability with no single point of failure.<br><br>

============================<br>
<b>5. Use Cases</b><br>
============================<br>

MongoDB:
- Real-time analytics
- Content management
- Web applications
- Mobile apps

Cassandra:
- Big data applications
- IoT systems
- High write throughput systems
- Distributed systems<br><br>

============================<br>
<b>6. Query Language</b><br>
============================<br>

MongoDB:
- Uses MongoDB Query Language (MQL).

Cassandra:
- Uses CQL (Cassandra Query Language), similar to SQL.<br><br>

============================<br>
<b>Summary:</b><br>
============================<br>

MongoDB → Flexible, developer-friendly, good for dynamic applications.<br>
Cassandra → Highly scalable, fault-tolerant, best for large distributed systems.<br>
`
    }
  ]
},
{
  id: "analyzing-queries",
  name: "Analyzing Queries in MongoDB",
  beginner: [
  {
    content: `
<b>Analyzing Queries in MongoDB</b><br><br>

Analyzing queries means checking how MongoDB executes a query.
It helps to understand performance and improve speed.<br><br>

MongoDB provides the explain() method for analyzing queries.<br><br>

<hr>

<b>1. explain() Method</b><br><br>

Used to see how MongoDB processes a query.<br><br>

<b>Syntax:</b><br>
<pre>
db.collectionName.find({ condition }).explain()
</pre>

<hr>

<b>2. Example</b><br><br>

<pre>
db.students.find({ name: "Pratiksha" }).explain("executionStats")
</pre>

This shows detailed execution information.<br><br>

<hr>

<b>3. Important Terms in explain()</b><br><br>

• COLLSCAN → Collection Scan (Slow, scans all documents)<br>
• IXSCAN → Index Scan (Fast, uses index)<br>
• executionTimeMillis → Time taken for query<br>
• totalDocsExamined → Number of documents checked<br><br>

<hr>

<b>4. Example with Index</b><br><br>

Step 1: Create Index<br>

<pre>
db.students.createIndex({ name: 1 })
</pre>

Step 2: Analyze Query<br>

<pre>
db.students.find({ name: "Pratiksha" }).explain("executionStats")
</pre>

If IXSCAN appears, index is used.<br><br>

<hr>

<b>Why Analyzing Queries is Important?</b><br><br>

• Improves performance<br>
• Helps detect slow queries<br>
• Helps decide where to create indexes<br><br>

<hr>

<b>Simple Concept</b><br><br>

Without Index → COLLSCAN → Slow<br>
With Index → IXSCAN → Fast<br><br>
`
  }
],
  intermediate: [
    {
      content: `
<b>Analyzing Queries in MongoDB:</b><br><br>

Query analysis helps to understand how MongoDB executes a query
and whether it is using indexes efficiently.<br><br>

MongoDB provides <b>explain()</b> method to analyze query performance.<br><br>

============================<br>
<b>1. explain()</b><br>
============================<br>

Used to display execution details of a query.<br><br>

<b>Syntax:</b>
<pre>
db.collection.find(query).explain()
</pre>

<b>Example:</b>
<pre>
db.students.find({ name: "Pratiksha" }).explain()
</pre>

This shows:
- Execution plan
- Whether index is used
- Number of documents scanned
- Number of documents returned<br><br>

============================<br>
<b>Execution Stages:</b><br>
============================<br>

<b>COLLSCAN:</b><br>
- Full collection scan.<br>
- No index used.<br>
- Slower performance.<br><br>

<b>IXSCAN:</b><br>
- Index scan.<br>
- Faster performance.<br><br>

============================<br>
<b>Important Fields in explain():</b><br>
============================<br>

- totalDocsExamined → Number of documents scanned<br>
- totalKeysExamined → Number of index keys scanned<br>
- executionTimeMillis → Time taken to execute query<br><br>

============================<br>
<b>Example (Using Index Efficiently):</b><br>
============================<br>

Create index:
<pre>
db.students.createIndex({ name: 1 })
</pre>

Then:
<pre>
db.students.find({ name: "Pratiksha" }).explain("executionStats")
</pre>

- Shows if query uses IXSCAN instead of COLLSCAN.<br><br>

============================<br>
<b>Why Query Analysis is Important?</b><br>
============================<br>

- Improves performance<br>
- Detects slow queries<br>
- Helps in index optimization<br>
- Reduces database load<br>
`
    }
  ]
},
{
  id: "map-reduce",
  name: "Map Reduce in MongoDB",
  beginner: [
  {
    content: `
<b>Map Reduce in MongoDB</b><br><br>

Map Reduce is a data processing technique used to perform aggregation
operations on large datasets.<br><br>

It works in two main steps:<br>
1. Map<br>
2. Reduce<br><br>

<hr>

<b>1. Map Function</b><br><br>

The map function processes each document and emits key-value pairs.<br><br>

<hr>

<b>2. Reduce Function</b><br><br>

The reduce function groups the values by key and performs calculation
like sum, average, count, etc.<br><br>

<hr>

<b>3. Basic Syntax</b><br><br>

<pre>
db.collectionName.mapReduce(
   mapFunction,
   reduceFunction,
   {
      out: "outputCollection"
   }
)
</pre>

<hr>

<b>4. Example</b><br><br>

Suppose we want to count students by city.<br><br>

<pre>
var mapFunction = function() {
   emit(this.city, 1);
};

var reduceFunction = function(key, values) {
   return Array.sum(values);
};

db.students.mapReduce(
   mapFunction,
   reduceFunction,
   { out: "cityCount" }
);
</pre>

This will create a new collection named cityCount
with total students per city.<br><br>

<hr>

<b>Important Points</b><br><br>

• Used for complex data processing.<br>
• Works on large datasets.<br>
• Slower compared to Aggregation Framework.<br>
• Aggregation is preferred in modern applications.<br><br>

<hr>

<b>Simple Concept</b><br><br>

Map → Select and emit data.<br>
Reduce → Combine and calculate result.<br><br>
`
  }
],
  intermediate: [
    {
      content: `
<b>Map Reduce in MongoDB:</b><br><br>

Map Reduce is a data processing technique used for large datasets.<br>
It processes data in two main phases:<br>
1. Map Phase<br>
2. Reduce Phase<br><br>

It is used for complex aggregations.<br><br>

============================<br>
<b>1. Map Function</b><br>
============================<br>

- Processes each document.<br>
- Emits key-value pairs.<br><br>

Example:
<pre>
var mapFunction = function() {
  emit(this.department, this.marks);
};
</pre>

============================<br>
<b>2. Reduce Function</b><br>
============================<br>

- Aggregates values for same key.<br><br>

Example:
<pre>
var reduceFunction = function(key, values) {
  return Array.sum(values);
};
</pre>

============================<br>
<b>Execute Map Reduce:</b><br>
============================<br>

<pre>
db.students.mapReduce(
  mapFunction,
  reduceFunction,
  { out: "totalMarks" }
)
</pre>

- Groups students by department.<br>
- Calculates total marks.<br>
- Stores result in "totalMarks" collection.<br><br>

============================<br>
<b>When to Use Map Reduce?</b><br>
============================<br>

- Large data processing<br>
- Complex aggregations<br>
- Custom calculations<br><br>

============================<br>
<b>Map Reduce vs Aggregation:</b><br>
============================<br>

Aggregation:
- Faster
- Uses pipeline framework
- Recommended in modern MongoDB

Map Reduce:
- Slower
- More flexible
- Used for advanced custom logic<br><br>

Note: Aggregation framework is preferred over Map Reduce in newer MongoDB versions.
`
    }
  ]
},
{
  id: "regular-expression",
  name: "Regular Expressions in MongoDB",
  beginner: [
  {
    content: `
<b>Regular Expressions in MongoDB</b><br><br>

Regular Expressions (Regex) are used to search text patterns in MongoDB.
They help to find matching strings inside documents.<br><br>

MongoDB uses $regex operator for pattern matching.<br><br>

<hr>

<b>1. Basic Syntax</b><br><br>

<pre>
db.collectionName.find({ field: { $regex: "pattern" } })
</pre>

<hr>

<b>2. Example – Find Name Starting with 'P'</b><br><br>

<pre>
db.students.find({ name: { $regex: "^P" } })
</pre>

^ → Starts with<br><br>

<hr>

<b>3. Example – Find Name Ending with 'a'</b><br><br>

<pre>
db.students.find({ name: { $regex: "a$" } })
</pre>

$ → Ends with<br><br>

<hr>

<b>4. Case-Insensitive Search</b><br><br>

<pre>
db.students.find({
   name: { $regex: "pratiksha", $options: "i" }
})
</pre>

i → Case insensitive<br><br>

<hr>

<b>5. Find Name Containing Word</b><br><br>

<pre>
db.students.find({ name: { $regex: "tik" } })
</pre>

This finds names containing "tik".<br><br>

<hr>

<b>Important Symbols</b><br><br>

^  → Start of string<br>
$  → End of string<br>
.  → Any single character<br>
*  → Zero or more characters<br><br>

<hr>

<b>Important Points</b><br><br>

• Regex is useful for pattern searching.<br>
• Case-insensitive search uses $options: "i".<br>
• Regex can be slow without proper indexing.<br><br>

<hr>

<b>Simple Concept</b><br><br>

Regex helps to search partial or pattern-based text data.<br><br>
`
  }
],
  intermediate: [
    {
      content: `
<b>Regular Expressions in MongoDB:</b><br><br>

Regular Expressions (Regex) are used to search for patterns in strings.<br>
MongoDB supports regex using <b>$regex</b> operator.<br><br>

============================<br>
<b>Syntax:</b><br>
============================<br>

<pre>
db.collection.find({ field: { $regex: "pattern" } })
</pre>

============================<br>
<b>Basic Example:</b><br>
============================<br>

<pre>
db.students.find({ name: { $regex: "Pra" } })
</pre>

- Finds students whose name contains "Pra".<br><br>

============================<br>
<b>Case-Insensitive Search:</b><br>
============================<br>

<pre>
db.students.find({
  name: { $regex: "pratiksha", $options: "i" }
})
</pre>

- "i" option makes search case-insensitive.<br><br>

============================<br>
<b>Starts With:</b><br>
============================<br>

<pre>
db.students.find({ name: { $regex: "^Pra" } })
</pre>

- ^ means starts with "Pra".<br><br>

============================<br>
<b>Ends With:</b><br>
============================<br>

<pre>
db.students.find({ name: { $regex: "sha$" } })
</pre>

- $ means ends with "sha".<br><br>

============================<br>
<b>Contains:</b><br>
============================<br>

<pre>
db.students.find({ name: { $regex: "tik" } })
</pre>

- Finds names containing "tik".<br><br>

============================<br>
<b>Using Regex Shortcut:</b><br>
============================<br>

<pre>
db.students.find({ name: /Pra/i })
</pre>

- Another way to write regex.<br><br>

============================<br>
<b>Important Notes:</b><br>
============================<br>

- Regex may not use index efficiently (unless prefix search).<br>
- Avoid using leading wildcard for performance.<br>
- Useful for search functionality.<br>
`
    }
  ]
},
{
  id: "capped-collection",
  name: "Capped Collection in MongoDB",
  beginner: [
  {
    content: `
<b>Capped Collection in MongoDB</b><br><br>

A Capped Collection is a fixed-size collection in MongoDB.
It automatically overwrites old documents when it reaches its size limit.<br><br>

It works like a circular queue (FIFO – First In First Out).<br><br>

<hr>

<b>1. Why Use Capped Collection?</b><br><br>

• To store log data<br>
• To store real-time data<br>
• When we need fixed storage size<br><br>

<hr>

<b>2. Create Capped Collection</b><br><br>

<b>Syntax:</b><br>
<pre>
db.createCollection("collectionName", {
   capped: true,
   size: sizeInBytes,
   max: maxDocuments
})
</pre>

<hr>

<b>3. Example</b><br><br>

<pre>
db.createCollection("logs", {
   capped: true,
   size: 100000,
   max: 100
})
</pre>

• size → Maximum storage size (in bytes)<br>
• max → Maximum number of documents (optional)<br><br>

<hr>

<b>4. Check if Collection is Capped</b><br><br>

<pre>
db.logs.isCapped()
</pre>

<hr>

<b>5. Important Behavior</b><br><br>

• Documents are stored in insertion order.<br>
• When size limit is reached, oldest documents are removed automatically.<br>
• Cannot delete documents manually from capped collection.<br><br>

<hr>

<b>Important Points</b><br><br>

• Best for logging and caching.<br>
• Fixed size improves performance.<br>
• Supports high-speed inserts.<br><br>

<hr>

<b>Simple Concept</b><br><br>

Normal Collection → Unlimited size.<br>
Capped Collection → Fixed size with automatic overwrite.<br><br>
`
  }
],
  intermediate: [
    {
      content: `
<b>Capped Collection in MongoDB:</b><br><br>

A capped collection is a fixed-size collection that automatically 
overwrites the oldest documents when it reaches its size limit.<br><br>

It works like a circular queue (FIFO – First In First Out).<br><br>

============================<br>
<b>Features:</b><br>
============================<br>

- Fixed size collection<br>
- Maintains insertion order<br>
- Very high performance<br>
- Automatically removes oldest records<br><br>

============================<br>
<b>Create Capped Collection:</b><br>
============================<br>

<pre>
db.createCollection("logs", {
  capped: true,
  size: 100000,
  max: 1000
})
</pre>

Parameters:<br>
- capped: true → Enables capped collection<br>
- size → Maximum size in bytes<br>
- max → Maximum number of documents (optional)<br><br>

============================<br>
<b>Check if Collection is Capped:</b><br>
============================<br>

<pre>
db.logs.isCapped()
</pre>

============================<br>
<b>Convert Normal Collection to Capped:</b><br>
============================<br>

<pre>
db.runCommand({
  convertToCapped: "logs",
  size: 100000
})
</pre>

============================<br>
<b>Use Cases:</b><br>
============================<br>

- Logging systems<br>
- Caching<br>
- Real-time analytics<br>
- Event tracking<br><br>

============================<br>
<b>Limitations:</b><br>
============================<br>

- Cannot delete specific documents<br>
- Cannot increase size after creation<br>
- Limited flexibility<br>
`
    }
  ]
},
{
  id: "redis-vs-mongodb",
  name: "Redis vs MongoDB",
  beginner: [
  {
    content: `
<b>Redis vs MongoDB</b><br><br>

Redis and MongoDB are both popular NoSQL databases,
but they are used for different purposes.<br><br>

<hr>

<b>1. Database Type</b><br><br>

MongoDB → Document-based database (stores JSON-like documents).<br>
Redis → In-memory key-value store.<br><br>

<hr>

<b>2. Data Storage</b><br><br>

MongoDB → Stores data on disk (persistent storage).<br>
Redis → Stores data in memory (RAM).<br><br>

<hr>

<b>3. Performance</b><br><br>

MongoDB → Fast for general-purpose applications.<br>
Redis → Extremely fast because data is stored in memory.<br><br>

<hr>

<b>4. Data Structure Support</b><br><br>

MongoDB → Documents (BSON format).<br>
Redis → Key-Value, Lists, Sets, Sorted Sets, Hashes.<br><br>

<hr>

<b>5. Use Cases</b><br><br>

MongoDB → Web applications, APIs, content management systems.<br>
Redis → Caching, session storage, real-time analytics.<br><br>

<hr>

<b>6. Scalability</b><br><br>

MongoDB → Supports sharding for horizontal scaling.<br>
Redis → Supports clustering for scaling.<br><br>

<hr>

<b>Comparison Table</b><br><br>

Feature        | MongoDB            | Redis
---------------------------------------------------
Type           | Document Database  | Key-Value Store
Storage        | Disk-based         | In-memory (RAM)
Speed          | Fast               | Very Fast
Best For       | Persistent data    | Caching & sessions
<br><br>

<hr>

<b>Important Points</b><br><br>

• MongoDB is used for storing application data.<br>
• Redis is mainly used as a cache or temporary storage.<br>
• Redis is faster but limited by RAM size.<br><br>

<hr>

<b>Simple Concept</b><br><br>

MongoDB → Store permanent data.<br>
Redis → Store temporary or frequently accessed data.<br><br>
`
  }
],
  intermediate: [
    {
      content: `
<b>Redis vs MongoDB:</b><br><br>

Redis and MongoDB are both NoSQL databases,
but they serve different purposes.<br><br>

============================<br>
<b>1. Data Model</b><br>
============================<br>

MongoDB:
- Document-oriented database.
- Stores data in BSON format.
- Supports complex documents.

Redis:
- In-memory key-value store.
- Stores data as strings, lists, sets, hashes.
- Very simple data model.<br><br>

============================<br>
<b>2. Storage</b><br>
============================<br>

MongoDB:
- Stores data on disk.
- Persistent by default.

Redis:
- Stores data in memory (RAM).
- Optional persistence support.<br><br>

============================<br>
<b>3. Performance</b><br>
============================<br>

MongoDB:
- Good performance for general-purpose applications.
- Suitable for large datasets.

Redis:
- Extremely fast (because in-memory).
- Ideal for caching and real-time systems.<br><br>

============================<br>
<b>4. Scalability</b><br>
============================<br>

MongoDB:
- Supports sharding for horizontal scaling.
- Replica sets for high availability.

Redis:
- Supports clustering.
- Used mainly for fast data access.<br><br>

============================<br>
<b>5. Use Cases</b><br>
============================<br>

MongoDB:
- Web applications
- Content management
- Real-time analytics
- Big data applications

Redis:
- Caching
- Session management
- Real-time leaderboards
- Message queues<br><br>

============================<br>
<b>Summary:</b><br>
============================<br>

MongoDB → Persistent, document-based database for large applications.<br>
Redis → In-memory, ultra-fast key-value store mainly used for caching.<br>
`
    }
  ]
},
{
  id: "mongodb-cloud",
  name: "MongoDB Cloud (MongoDB Atlas)",
  beginner: [
  {
    content: `
<b>MongoDB Cloud (MongoDB Atlas)</b><br><br>

MongoDB Atlas is a cloud-based database service provided by MongoDB.
It allows you to create, manage, and scale MongoDB databases in the cloud.<br><br>

You do not need to install MongoDB locally.
Atlas manages servers, backups, security, and scaling automatically.<br><br>

<hr>

<b>1. What is MongoDB Atlas?</b><br><br>

• Fully managed cloud database service<br>
• Hosted on AWS, Azure, and Google Cloud<br>
• Easy setup and maintenance<br><br>

<hr>

<b>2. Key Features</b><br><br>

• Automatic backups<br>
• Built-in security<br>
• Auto-scaling<br>
• Monitoring tools<br>
• High availability (Replica Sets)<br><br>

<hr>

<b>3. Steps to Create Free Cluster</b><br><br>

Step 1: Go to MongoDB Atlas website<br>
Step 2: Create account / Login<br>
Step 3: Create new project<br>
Step 4: Build a free cluster (M0 cluster)<br>
Step 5: Add database user and IP address<br>
Step 6: Connect using connection string<br><br>

<hr>

<b>4. Example Connection String</b><br><br>

<pre>
mongodb+srv://username:password@cluster0.mongodb.net/CollegeDB
</pre>

This string is used in applications (Node.js, Spring Boot, etc.)<br><br>

<hr>

<b>5. Advantages of MongoDB Atlas</b><br><br>

• No server management required<br>
• Easy scaling<br>
• Cloud-based access<br>
• Suitable for production applications<br><br>

<hr>

<b>Simple Concept</b><br><br>

Local MongoDB → Installed on your computer.<br>
MongoDB Atlas → Hosted in cloud and managed automatically.<br><br>
`
  }
],
  intermediate: [
    {
      content: `
<b>MongoDB Cloud (MongoDB Atlas):</b><br><br>

MongoDB Atlas is the fully managed cloud database service provided by MongoDB.<br>
It allows users to deploy, manage, and scale MongoDB in the cloud.<br><br>

============================<br>
<b>Features:</b><br>
============================<br>

- Fully managed database<br>
- Automatic backups<br>
- Built-in security<br>
- Auto-scaling<br>
- Monitoring and alerts<br>
- Multi-cloud support (AWS, Azure, GCP)<br><br>

============================<br>
<b>Why Use MongoDB Atlas?</b><br>
============================<br>

- No need to install MongoDB locally<br>
- Easy deployment<br>
- High availability<br>
- Automatic replication<br>
- Secure cloud environment<br><br>

============================<br>
<b>Steps to Create MongoDB Cloud Database:</b><br>
============================<br>

1. Create account on MongoDB Atlas website.<br>
2. Create a new cluster.<br>
3. Choose cloud provider (AWS / Azure / GCP).<br>
4. Create database user and password.<br>
5. Whitelist IP address.<br>
6. Connect using connection string.<br><br>

============================<br>
<b>Connection String Example:</b><br>
============================<br>

<pre>
mongodb+srv://username:password@cluster0.mongodb.net/myDatabase
</pre>

Used in:
- Node.js
- Spring Boot
- Java applications
- React applications<br><br>

============================<br>
<b>Advantages:</b><br>
============================<br>

- Easy scaling<br>
- Automatic backups<br>
- Global cluster support<br>
- Reduced maintenance<br><br>

============================<br>
<b>Use Cases:</b><br>
============================<br>

- Production applications<br>
- Cloud-native apps<br>
- Microservices architecture<br>
- Global applications<br>
`
    }
  ]
}

        ]
      }
    ],

    // ===== Cheat Notes =====
    cheatNotes: [
      "MongoDB is NoSQL database",
      "Data stored in BSON format",
      "Collection = Table",
      "Document = Row",
      "_id acts as primary key",
      "Supports dynamic schema",
      "Highly scalable and flexible"
    ],

    // ===== Interview Questions =====
    interviewQuestions: [
      {
        company: "TCS",
        questions: [
          "What is MongoDB?",
          "Difference between MongoDB and SQL?",
          "What is BSON?",
          "Explain Collection and Document"
        ]
      },
      {
        company: "Infosys",
        questions: [
          "Explain MongoDB Architecture",
          "Advantages of MongoDB",
          "Explain CRUD operations",
          "What is indexing in MongoDB?"
        ]
      }
    ]
  }
];
